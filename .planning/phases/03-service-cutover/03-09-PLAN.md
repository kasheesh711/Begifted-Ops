---
phase: 03-service-cutover
plan: 09
type: execute
wave: 5
depends_on:
  - 03-08
files_modified:
  - web/package.json
  - web/package-lock.json
requirements:
  - SVC-06
autonomous: true
must_haves:
  truths:
    - "googleapis dependency is absent from web/package.json"
    - "compare-live, ensure-action-sheets, ensure-inactive-sheet npm scripts are absent from web/package.json"
    - "web/package-lock.json reflects the dependency removal (no longer references googleapis or its transitive deps unique to googleapis)"
    - "cd web && npm run build exits 0 with the leaner dependency set"
    - "cd web && npm test -- --run exits 0 with 176 tests"
    - "Bundle size measurably smaller (informational — no specific byte target)"
    - "Post-merge gate verification (operator admin seed run, pre-deploy cache tag purge, manual QA click-through) belongs to 03-10, NOT 03-09 — this plan only proves the dependency-removal commit is buildable"
  artifacts:
    - path: "web/package.json"
      provides: "Cleaned manifest without googleapis or Sheets-era scripts"
      contains: "(absence of googleapis key)"
    - path: "web/package-lock.json"
      provides: "Updated lock without googleapis tree"
      contains: "(absence of googleapis package entries)"
  key_links:
    - from: "post-cutover repo"
      to: "Vercel build pipeline"
      via: "npm ci uses cleaned package-lock"
      pattern: "(no key_link, deletion-only)"
---

<objective>
Remove `googleapis` from `web/package.json` dependencies and drop the 3 Sheets-era npm scripts (`compare-live`, `ensure-action-sheets`, `ensure-inactive-sheet`) whose source files were deleted in Plan 03-08 (SVC-06). Refresh `package-lock.json` via `npm install` so the lockfile no longer contains googleapis or its transitive deps.

Purpose: Per D-33 commit-9 ordering, dependency removal lands AFTER source-file deletion (Plan 03-08). At this point in the PR sequence, no source file imports `googleapis` (Plan 03-03 + 03-05 + 03-08 collectively removed every reference). Removing the dep is the final cleanup commit — the build proves no live import remains AND the bundle size shrinks.
Output: Cleaner `package.json`, refreshed `package-lock.json`. Vercel deploys faster. The 30-day Apps Script tail (Phase 5) doesn't need googleapis on the Next.js side; clasp + .gs files keep their own toolchain.

Scope boundary: This plan ends at "build green + tests green + lockfile pruned." The pre-merge operator gates (admin seed run per D-31, pre-deploy cache tag purge per RESEARCH §A8, manual QA click-through per D-35 gate 4) are NOT this plan's responsibility — they live in Plan 03-10 (Wave 6 Pre-Merge Gate) and run AFTER all 9 code commits land. 03-09 must be buildable on its own; 03-10 is the cutover-day operator checklist that blocks the merge button.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-service-cutover/03-CONTEXT.md
@.planning/phases/03-service-cutover/03-PATTERNS.md
@web/package.json
@web/package-lock.json

Current `web/package.json` dependencies (informational, derived from CLAUDE.md §Stack):

- `googleapis@171.4.0` — REMOVE
- `next@16.2.1` — KEEP
- `next-auth@5.0.0-beta.30` — KEEP
- `react@19.2.4`, `react-dom@19.2.4` — KEEP
- `@neondatabase/serverless`, `drizzle-orm`, `zod`, `@vercel/functions`, `p-limit`, `ws` — KEEP (Phase 2)

Current `web/package.json` scripts:

- `dev`, `build`, `start`, `test`, `test:watch` — KEEP
- `compare-live` — REMOVE (source deleted in Plan 03-08)
- `ensure-action-sheets` — REMOVE (source deleted in Plan 03-08)
- `ensure-inactive-sheet` — REMOVE (source deleted in Plan 03-08)
- `db:migrate`, `db:seed-admin` — KEEP (Phase 2)
- `lint:no-revalidate-max` — KEEP (Phase 3 Plan 03-03)

Cross-file rule guarding this removal: Pattern Map §Cross-File Consistency Constraints rule #9 — "googleapis removed in commit 9 (D-33) — only after every import of it is gone."
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-removal googleapis import audit</name>
  <read_first>
    - .planning/phases/03-service-cutover/03-PATTERNS.md §Cross-File Consistency Constraints rule #9
  </read_first>
  <files></files>
  <action>
Before editing package.json, prove there are zero googleapis imports anywhere in the source tree. Plan 03-08 deleted every consumer; this task is the safety net.

Run from web/:

```
cd web
grep -rn "from \"googleapis" src/ --include='*.ts' --include='*.tsx' || echo "OK: no googleapis imports in src/"
grep -rn "import.*googleapis" scripts/ --include='*.ts' --include='*.js' || echo "OK: no googleapis imports in scripts/"
grep -rn "require.*googleapis" src/ scripts/ --include='*.ts' --include='*.js' || echo "OK: no googleapis require() calls"
```

EXPECTED: All 3 greps return "OK: ..." messages with zero matches.

If ANY match exists, STOP. Plan 03-08 didn't fully delete a consumer. Revise via gap-closure mode before proceeding.
  </action>
  <acceptance_criteria>
    - The first grep above returns 0 matches in src/ tree.
    - The second grep above returns 0 matches in scripts/ tree.
    - The third grep above returns 0 matches across both trees.
  </acceptance_criteria>
  <verify>
    <automated>cd "/Users/kevinhsieh/Developer/Credit Control/Begifted-Ops/web" &amp;&amp; [ -z "$(grep -rn 'from \"googleapis' src/ --include='*.ts' --include='*.tsx' 2>/dev/null)" ] &amp;&amp; [ -z "$(grep -rn 'import.*googleapis' scripts/ --include='*.ts' --include='*.js' 2>/dev/null)" ] &amp;&amp; [ -z "$(grep -rn 'require.*googleapis' src/ scripts/ --include='*.ts' --include='*.js' 2>/dev/null)" ]</automated>
  </verify>
  <done>
Audit confirms zero remaining googleapis imports; safe to remove the dep.
  </done>
</task>

<task type="auto">
  <name>Task 2: Edit package.json (remove googleapis dep + 3 scripts)</name>
  <read_first>
    - web/package.json (full file — confirm exact key locations to remove)
    - Task 1 acceptance criteria — must be green before this task runs
  </read_first>
  <files>
    web/package.json
  </files>
  <action>
Edit web/package.json to remove:

1. The `"googleapis"` key from the `"dependencies"` object.
2. The 3 npm scripts from the `"scripts"` object:
   - `"compare-live"`
   - `"ensure-action-sheets"`
   - `"ensure-inactive-sheet"`

Preserve every other dependency, devDependency, and script entry verbatim. Do NOT bump versions of unrelated packages. Do NOT reorder remaining keys (npm reads JSON regardless, but smaller PR diffs matter for review).

Use the Edit tool (not a sed/awk one-liner). Confirm post-edit JSON validity by parsing the file with `node -e "JSON.parse(require('fs').readFileSync('web/package.json','utf8'))"`.
  </action>
  <acceptance_criteria>
    - `node -e "process.exit(JSON.parse(require('fs').readFileSync('web/package.json','utf8')).dependencies.googleapis === undefined ? 0 : 1)"` exits 0 (googleapis key absent).
    - `node -e "const p=JSON.parse(require('fs').readFileSync('web/package.json','utf8')); process.exit((p.scripts['compare-live']===undefined && p.scripts['ensure-action-sheets']===undefined && p.scripts['ensure-inactive-sheet']===undefined) ? 0 : 1)"` exits 0 (3 scripts absent).
    - Other deps still present: `node -e "const p=JSON.parse(require('fs').readFileSync('web/package.json','utf8')); process.exit((p.dependencies.next && p.dependencies['next-auth'] && p.dependencies['drizzle-orm']) ? 0 : 1)"` exits 0.
    - Other scripts still present: `node -e "const p=JSON.parse(require('fs').readFileSync('web/package.json','utf8')); process.exit((p.scripts['db:migrate'] && p.scripts['db:seed-admin'] && p.scripts['lint:no-revalidate-max']) ? 0 : 1)"` exits 0.
    - File still parses as valid JSON: `node -e "JSON.parse(require('fs').readFileSync('web/package.json','utf8'))"` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd "/Users/kevinhsieh/Developer/Credit Control/Begifted-Ops/web" &amp;&amp; node -e "const p=JSON.parse(require('fs').readFileSync('package.json','utf8')); if (p.dependencies.googleapis !== undefined) process.exit(1); if (p.scripts['compare-live'] || p.scripts['ensure-action-sheets'] || p.scripts['ensure-inactive-sheet']) process.exit(1); if (!p.dependencies.next || !p.dependencies['next-auth']) process.exit(1); if (!p.scripts['db:migrate'] || !p.scripts['db:seed-admin'] || !p.scripts['lint:no-revalidate-max']) process.exit(1); console.log('OK');"</automated>
  </verify>
  <done>
package.json no longer declares googleapis or 3 Sheets-era scripts; all Phase 2 + Phase 3 entries preserved.
  </done>
</task>

<task type="auto">
  <name>Task 3: Refresh package-lock.json + run build + tests</name>
  <read_first>
    - Task 2 acceptance criteria — must be green
    - web/package-lock.json (do NOT manually edit; npm install regenerates)
  </read_first>
  <files>
    web/package-lock.json
  </files>
  <action>
Run `npm install` (NOT `npm ci`) inside `web/` so the lockfile prunes googleapis and its transitive deps. Then run the full build + test gates to prove the cleanup didn't break anything.

Steps:

1. `cd web && npm install` — regenerates package-lock.json. Expect npm to print "removed N packages" where N is the size of the googleapis tree (typically 30-60 packages).
2. `cd web && npx tsc --noEmit` — exit 0 (proves no orphan type imports survived).
3. `cd web && npm run build` — exit 0 (proves no orphan runtime imports either; Next.js production build catches dynamic imports tsc misses).
4. `cd web && npm test -- --run` — exit 0; expect 176 tests preserved (164 baseline + 5 health-route from Plan 03-06 + 4 cache-invalidation skipped + 3 chunked-transfer from Plan 03-07).
5. `cd web && npm run lint:no-revalidate-max` — exit 0.

Git tracks the regenerated lockfile diff; commit it alongside package.json in the same commit (D-33 commit-9 atomicity).
  </action>
  <acceptance_criteria>
    - `cd web && npx tsc --noEmit` exits 0.
    - `cd web && npm run build` exits 0.
    - `cd web && npm test -- --run` exits 0; final line shows total 176 tests passed (or 172 passed + 4 skipped if vitest reports skipped separately — match Plan 03-07 acceptance).
    - `cd web && npm run lint:no-revalidate-max` exits 0.
    - `grep -c '"googleapis"' web/package-lock.json` returns 0 (no remaining lockfile entries — note: this grep would match a transitive dep that names "googleapis-common" or similar; if so, narrow to `'"node_modules/googleapis":'` or `'"googleapis":'` exactly).
    - More precise: `node -e "const lock = JSON.parse(require('fs').readFileSync('web/package-lock.json','utf8')); const hasGoogleapis = Object.keys(lock.packages || {}).some(k => k === 'node_modules/googleapis' || k === ''); process.exit((!hasGoogleapis || !lock.packages['node_modules/googleapis']) ? 0 : 1)"` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd "/Users/kevinhsieh/Developer/Credit Control/Begifted-Ops/web" &amp;&amp; npm install &amp;&amp; npx tsc --noEmit &amp;&amp; npm run build &amp;&amp; npm test -- --run &amp;&amp; bash scripts/lint-no-revalidate-max.sh &amp;&amp; node -e "const lock = JSON.parse(require('fs').readFileSync('package-lock.json','utf8')); const has = lock.packages &amp;&amp; lock.packages['node_modules/googleapis']; process.exit(has ? 1 : 0)"</automated>
  </verify>
  <done>
package-lock.json refreshed; googleapis tree pruned; build green; 176 tests pass; lint green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo state pre-cutover-PR-merge → post-cutover-PR-merge | Dependency removal is reversible via git revert + npm install but compounds with Plan 03-08 deletion (one-way door). |
| package-lock.json → Vercel `npm ci` build step | Lock changes propagate to next deploy automatically; Vercel's npm ci will use the smaller dep tree. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-36 | Tampering | Premature googleapis removal (consumer still imports) | mitigate | Task 1 grep audit verifies zero imports BEFORE Task 2 edits. Task 3 build step catches any dynamic import that grep missed. Three-layer safety. |
| T-03-37 | Tampering | Stranded transitive googleapis dep accidentally promoted to direct | mitigate | npm install (NOT npm ci) re-resolves the tree from scratch. Any package that was BOTH a direct googleapis dep AND a transitive dep of something we keep stays installed under the keeper. Phase 2's `ws` is the canary case (we explicitly added it as direct in 02-05); other transitives prune cleanly. |
| T-03-38 | Information Disclosure | Lockfile churn revealing internal version pins | accept | package-lock.json is committed (CLAUDE.md §Stack confirms 3,222-line lockfile is tracked). Removing googleapis is a public, intentional action documented in the PR. No new secrets introduced. |
| T-03-39 | Repudiation | Vercel build failing post-merge because npm ci uses the new lock against an unupdated production cache | accept | Vercel rebuilds the npm cache per deploy; the lockfile diff propagates atomically. Standard Vercel deploy semantics. |
</threat_model>

<verification>
Scope: this plan only verifies the code-side state of commit 9 (the dependency-removal commit). Operator-side pre-merge gates live in Plan 03-10.

- `cd web && npx tsc --noEmit` exits 0
- `cd web && npm run build` exits 0
- `cd web && npm test -- --run` exits 0; 176 tests preserved
- `cd web && npm run lint:no-revalidate-max` exits 0
- `node -e "..."` confirms package-lock.json no longer has a `node_modules/googleapis` entry

Out of scope here (handled by Plan 03-10 Wave 6 Pre-Merge Gate):
- D-31 admin seed run + row count documented in PR description
- RESEARCH §A8 pre-deploy cache tag purge from Vercel dashboard
- D-35 gate 4 manual QA click-through on Vercel preview URL
</verification>

<success_criteria>
- Maps to ROADMAP Phase 3 SC#3 second half ("`googleapis` is removed from `web/package.json` — production build succeeds with zero references").
- Final Phase 3 code commit per D-33 commit-9 ordering. (Plan 03-10's Pre-Merge Gate is operator-only — it does NOT add a tenth code commit; it gates the merge button.)
</success_criteria>

<rollback>
Per D-27, Vercel "Promote Deployment" of the prior production build is the first-line rollback. The prior deploy still has googleapis@171.4.0 in its lockfile and will run unchanged.

Per-plan git revert: reverting the package.json + package-lock.json commit restores googleapis as a direct dep and re-adds the 3 npm scripts. But Plan 03-08 already deleted the script source files, so `npm run compare-live` would fail with "file not found" until a deeper revert (Plan 03-08 + 03-09 together) restores the source files too.

Cleanest rollback for Phase 3 = Vercel-dashboard promote-previous-deploy. Cleanest forward-fix = revert the cutover-PR merge commit (preserves merge structure per D-33) and re-do the rewire from Plan 03-03 onward.
</rollback>

<output>
After completion, create `.planning/phases/03-service-cutover/03-09-SUMMARY.md` capturing:
- Pre-removal grep audit evidence (output of Task 1)
- npm install output line "removed N packages" (informational; expect 30-60)
- Final test count (expect 176 preserved)
- Lockfile size before/after (informational)
- Confirmation that `web/package.json` is valid JSON post-edit and contains expected keep-list deps + scripts
- Cross-reference: Phase 5 RETI-06 will update `CLAUDE.md` / `AGENTS.md` / `docs/WORKFLOW.md` to remove `npm run compare-live` from the validation-run rules (the npm script is gone but doc references remain until Phase 5)
- Cross-reference: post-merge gate verification belongs to 03-10 (admin seed + cache purge + manual QA), not to this plan's SUMMARY
</output>
</output>
