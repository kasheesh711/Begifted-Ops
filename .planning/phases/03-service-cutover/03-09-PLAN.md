---
phase: 03-service-cutover
plan: "09"
type: execute
wave: 5
depends_on: ["03-07"]
files_modified:
  - web/package.json
  - web/package-lock.json
autonomous: true
requirements:
  - SVC-06

must_haves:
  truths:
    - "googleapis is NOT in web/package.json dependencies"
    - "package-lock.json is updated to reflect googleapis removal"
    - "npm run build succeeds after dep removal"
    - "npm test -- --run exits 0 after dep removal"
  artifacts:
    - path: "web/package.json"
      provides: "Clean package.json without googleapis"
  key_links:
    - from: "web/package.json googleapis removal"
      to: "build passes"
      via: "zero imports of googleapis in remaining web/src/ files"
      pattern: "!grep -r googleapis web/src/"
---

<objective>
Remove `googleapis` from `web/package.json` and regenerate `package-lock.json`. This is the final dependency cleanup after the Sheets-era code was deleted in Wave 5 (03-08).

Purpose: After lib/sheets/ is deleted, googleapis has no consumers. Removing it from the manifest reduces the attack surface and bundle size.

Note: The three npm scripts that referenced Sheets tooling (compare-live, ensure-action-sheets, ensure-inactive-sheet) were removed in 03-08. This plan only needs to remove the googleapis package itself.
</objective>

<execution_context>
@/Users/kevinhsieh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kevinhsieh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kevinhsieh/Developer/Credit Control/Begifted-Ops/.planning/ROADMAP.md
@/Users/kevinhsieh/Developer/Credit Control/Begifted-Ops/.planning/phases/03-service-cutover/03-CONTEXT.md

Current state of package.json after 03-08:
- "compare-live", "ensure-action-sheets", "ensure-inactive-sheet" scripts already removed
- "googleapis" 171.4.0 still in dependencies (it was imported by lib/sheets/ which is now deleted)

googleapis version in current package.json: 171.4.0 (per CLAUDE.md §Key Dependencies)

After removal, there must be zero imports of googleapis in any remaining web/src/ file.
RESEARCH.md confirmed: "no remaining server-side consumers post-cutover" (SVC-06).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove googleapis dependency and refresh lockfile</name>
  <files>
    web/package.json
    web/package-lock.json
  </files>
  <read_first>
    1. web/package.json — read to confirm googleapis appears in dependencies (not devDependencies)
       and to see current version
    2. Run: grep -r "googleapis" web/src/ --include="*.ts" --include="*.tsx" -l
       (must return empty — if any file still imports googleapis, DO NOT REMOVE; fix that import first)
  </read_first>
  <action>
    AUDIT: Confirm zero googleapis references in web/src/:
    grep -r "googleapis" web/src/ --include="*.ts" --include="*.tsx" -l
    (must return empty — if not, stop and fix remaining references from lib/sheets/ which should
    have been deleted in 03-08; if 03-08 ran successfully, this will be empty)

    Remove the dependency:
    cd web && npm uninstall googleapis

    This command:
    1. Removes "googleapis" from web/package.json dependencies
    2. Regenerates web/package-lock.json to remove googleapis and its transitive deps

    After uninstall:
    1. Verify: grep '"googleapis"' web/package.json | wc -l  (must be 0)
    2. Run: cd web && npm run build (must exit 0)
    3. Run: cd web && npx tsc --noEmit (must exit 0)
    4. Run: cd web && npm test -- --run (must exit 0)

    Commit:
    git add web/package.json
    git add web/package-lock.json
    git commit -m "chore(03-09): remove googleapis dep (Sheets-era — no remaining consumers)"
  </action>
  <verify>
    <automated>
      grep '"googleapis"' web/package.json | wc -l
      # (must be 0)
      grep -r "googleapis" web/src/ --include="*.ts" --include="*.tsx" | wc -l
      # (must be 0)
      cd web && npm run build && echo "build: OK"
      cd web && npm test -- --run && echo "tests: OK"
    </automated>
  </verify>
  <done>
    googleapis not in package.json. Zero references in web/src/. Build passes. Tests pass.
    package.json and package-lock.json committed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package.json → npm install | Removing a dep must not leave dangling imports |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09-1 | Denial of Service | removing googleapis breaks a hidden consumer | mitigate | Pre-removal grep audit confirms zero imports. `npm run build` after removal catches any missed references at compile time (TypeScript type imports would also fail). |
</threat_model>

<verification>
1. `grep '"googleapis"' web/package.json | wc -l` returns 0
2. `grep -r "googleapis" web/src/ --include="*.ts" --include="*.tsx" | wc -l` returns 0
3. `cd web && npm run build` exits 0
4. `cd web && npm test -- --run` exits 0
5. `git show HEAD --name-only | grep "package.json"` exits 0 (package.json in commit)
</verification>

<success_criteria>
- googleapis removed from web/package.json
- package-lock.json updated (googleapis and its transitive deps gone)
- Zero remaining googleapis imports in web/src/
- npm run build exits 0
- npm test -- --run exits 0
- Commit message: chore(03-09): remove googleapis dep (Sheets-era — no remaining consumers)
</success_criteria>

<output>
After completion, create `.planning/phases/03-service-cutover/03-09-SUMMARY.md` with:
- googleapis version removed
- Any unexpected transitive deps also removed (visible in package-lock.json diff)
- Build and test results
- Commit SHA
</output>
