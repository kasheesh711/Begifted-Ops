---
phase: 03-service-cutover
plan: "10"
type: execute
wave: 7
depends_on: ["03-08", "03-09"]
files_modified: []
autonomous: false
requirements:
  - SVC-01
  - SVC-02
  - SVC-03
  - SVC-04
  - SVC-05
  - SVC-06
  - SVC-07
  - SVC-08
  - TEST-06

user_setup:
  - service: vercel
    why: "Prod env pull + admin-ownership seed against production Neon"
    env_vars:
      - name: DATABASE_URL_UNPOOLED
        source: "Vercel Dashboard linked to Neon Marketplace OR run: cd web && vercel env pull .env"
    dashboard_config:
      - task: "Verify Neon is provisioned via Vercel Marketplace (auto-injects DATABASE_URL vars)"
        location: "Vercel Dashboard → Project → Integrations → Neon"
      - task: "Verify WISENET_BASE_URL, WISENET_USER_ID, WISENET_API_KEY, WISENET_CENTER_ID, WISENET_NAMESPACE are set in Vercel production environment"
        location: "Vercel Dashboard → Project → Settings → Environment Variables"
      - task: "Confirm prior production deployment (dpl_9dvQvrEe6RB1ZaEE9MN1ooCx2REV) is reachable in Vercel Deployments tab"
        location: "Vercel Dashboard → Project → Deployments"
  - service: neon
    why: "Admin-ownership seed requires unpooled connection to production Neon branch"
    env_vars:
      - name: DATABASE_URL_UNPOOLED
        source: "After vercel env pull: cat web/.env | grep DATABASE_URL_UNPOOLED"
  - service: github
    why: "DB-migrate CI job must be green on the cutover PR's preview branch"
    dashboard_config:
      - task: "Open the cutover PR and confirm .github/workflows/db-migrate.yml CI check is green"
        location: "GitHub → Repository → Pull Requests → cutover PR → Checks"

must_haves:
  truths:
    - "npm test -- --run exits 0 with 180+ tests passing"
    - "npx tsc --noEmit exits 0"
    - "npm run build exits 0"
    - "npm run lint:no-revalidate-max exits 0 with empty allowlist"
    - "db-migrate CI job green on PR preview branch"
    - "Admin-ownership seed ran successfully against production Neon with INSERT count documented in PR"
    - "Operator manual QA passed on Vercel preview URL"
    - "prod-snapshot transfer to Begifted-Ops-prod-snapshot/web/ committed and pushed"
    - "Vercel auto-deploy triggered and production live on new build"
  artifacts:
    - path: "Begifted-Ops-prod-snapshot/web/src/lib/dashboard/service.ts"
      provides: "Production copy of new service.ts (post-transfer)"
      contains: "use cache: remote"
    - path: "Begifted-Ops-prod-snapshot/web/next.config.ts"
      provides: "Production copy of next.config.ts with cacheComponents: true"
  key_links:
    - from: "Begifted-Ops/web/"
      to: "Begifted-Ops-prod-snapshot/web/"
      via: "operator rsync/cp transfer (cutover diff applied to prod-snapshot)"
      pattern: "rsync -av --exclude .git"
---

<objective>
Pre-Merge Gate (operator-only, autonomous: false). Four gate items from D-35, plus admin-ownership seed (D-31), cache tag purge (RESEARCH §A8), and the prod-snapshot transfer (D-42).

Purpose: This plan is a structured checklist ensuring Phase 3 is production-ready before the cutover diff goes live. The operator executes each gate item in sequence and documents evidence. Only after all gates pass does the prod-snapshot transfer happen, triggering the Vercel auto-deploy.

IMPORTANT: TEST-04 (50-concurrent load test) is DEFERRED to Phase 4 DEPL-04 per D-34. It does not appear in this gate. Vercel preview QA (D-35 step 4) covers functional correctness; p95 latency testing deferred to Phase 4.

Output: Production deployment of the Wisenet+Postgres cutover via Begifted-Ops-prod-snapshot → Vercel.
</objective>

<execution_context>
@/Users/kevinhsieh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kevinhsieh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kevinhsieh/Developer/Credit Control/Begifted-Ops/.planning/ROADMAP.md
@/Users/kevinhsieh/Developer/Credit Control/Begifted-Ops/.planning/phases/03-service-cutover/03-CONTEXT.md

D-27 rollback: Vercel dashboard → Deployments → click Promote Deployment on prior build.
  Prior build is dpl_9dvQvrEe6RB1ZaEE9MN1ooCx2REV (the Sheets-era prod deploy).
  This must be reachable before cutover.

D-31 admin-ownership seed:
  1. vercel env pull web/.env (pulls DATABASE_URL_UNPOOLED from Vercel/Neon)
  2. npm run db:seed-admin
  3. Expected output: "[seed-admin-ownership] Parsed N ownership entries. OK — inserted/updated N rows."
  4. Document N in PR description under "Seed evidence" section

D-35 gate checklist (ALL 4 must pass):
  1. Automated bundle check (npm test, tsc, build, lint)
  2. DB-migrate CI green on Neon preview branch
  3. Admin seed ran against production Neon
  4. Operator manual QA on Vercel preview URL

D-42 prod-snapshot transfer:
  After all D-35 gates pass + manual QA + seed:
  1. rsync Begifted-Ops/web/ to Begifted-Ops-prod-snapshot/web/ (excluding .git, .vercel, node_modules)
  2. Resolve conflicts in favor of Begifted-Ops/web/ for analytics.ts, env.ts, dashboard-logic.test.ts
  3. Commit to Begifted-Ops-prod-snapshot/.git
  4. Push to kasheesh711/begifted-credit-dashboard main
  5. Vercel auto-deploys from the GitHub push

D-42 conflict resolution policy:
  - analytics.ts: Begifted-Ops/web/ version WINS (has Phase 2 getWisenetEnv work)
  - env.ts: Begifted-Ops/web/ version WINS (has getWisenetEnv(), getDbEnv())
  - dashboard-logic.test.ts: Begifted-Ops/web/ version WINS (has Wisenet parity block)
  - All other files: Begifted-Ops/web/ version WINS (they are the cutover deliverables)

RESEARCH §A8 cache tag purge:
  If the prior prod deploy cached any "dashboard-payload" tag entries in Vercel Runtime Cache,
  they will be served until TTL expires (max 300s = 5 minutes). This is acceptable.
  No manual purge action required — the new deploy will generate fresh cache entries.
  Document in PR description: "Cache entry TTL: up to 5 minutes post-deploy for stale Sheets entries."
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Gate 1: Automated bundle health check (D-35 item 1)</name>
  <what-built>
    Claude executor will run all 4 automated checks from the Begifted-Ops/web/ directory.
    These must all pass before proceeding to the remaining gates.
  </what-built>
  <action>
    Run each command. All must exit 0. If any fail, abort and fix before continuing.

    1. cd web && npm test -- --run
       Expected: 180+ tests pass, 0 failures (includes all Phase 3 new tests)
       cache-invalidation.test.ts should SKIP (no TEST_DATABASE_URL in local env — that is OK)

    2. cd web && npx tsc --noEmit
       Expected: no errors (0 output)

    3. cd web && npm run build
       Expected: successful Next.js production build; no "use cache: remote" compile errors

    4. cd web && npm run lint:no-revalidate-max
       Expected: exits 0 with "No deprecated single-arg revalidateTag() calls found" or similar
       The allowlist must be EMPTY (emptied in 03-03; service.ts no longer has the old anti-pattern)

    All 4 commands must exit 0. Record output of each.
  </action>
  <how-to-verify>
    Run each command listed above from /Users/kevinhsieh/Developer/Credit Control/Begifted-Ops/
    Note test count and any warnings. Confirm:
    - Test count >= 180 (Phase 2 baseline was 164; Phase 3 adds 5+ tests)
    - Zero TypeScript errors
    - Successful Next.js build output (no errors, only standard webpack/turbopack output)
    - Lint exits 0
  </how-to-verify>
  <resume-signal>
    Type "gate-1-passed" after all 4 commands exit 0 and you have recorded the test count.
    Or describe which check failed for debugging.
  </resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Gate 2: DB-migrate CI green on Neon preview branch (D-35 item 2)</name>
  <what-built>
    The .github/workflows/db-migrate.yml CI workflow was added in Phase 2 (02-10).
    It runs automatically on push to main when drizzle/ or schema.ts files change.
  </what-built>
  <action>
    The cutover PR must have been raised on a branch before merging. Verify the CI job is green.

    If working directly on main (per branching_strategy: none):
    1. Check: git log --oneline -15 to see if db-migrate workflow was triggered
    2. Go to GitHub → kasheesh711/begifted-credit-dashboard (WAIT — this is the wrong repo)
       Actually: Go to GitHub → the Begifted-Ops repo's Actions tab
    3. Find the most recent "db-migrate" workflow run
    4. Confirm it completed successfully (green checkmark)

    If no recent run exists (because drizzle/ files weren't modified in Phase 3):
    This gate is considered satisfied if:
    - Phase 2 db-migrate CI ran successfully (confirmed in Phase 2 completion)
    - Phase 3 added no new migrations (no drizzle/ changes in Phase 3)
    Document this rationale in the PR description: "No new migrations in Phase 3 — Phase 2 CI run confirms schema is current."

    VERCEL PREVIEW: If a Vercel preview URL exists for the current changes, use it for Gate 4 below.
    Note the preview URL here.
  </action>
  <how-to-verify>
    Navigate to the GitHub Actions tab and confirm the db-migrate job is green.
    OR confirm no new migrations exist in Phase 3 (no drizzle/ files changed since Phase 2).
  </how-to-verify>
  <resume-signal>
    Type "gate-2-passed" with either:
    - The GitHub Actions run URL (green)
    - "No new migrations in Phase 3 — Phase 2 CI confirmed at [date]"
  </resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Gate 3: Admin-ownership seed against production Neon (D-31, D-35 item 3)</name>
  <what-built>
    web/scripts/seed-admin-ownership.ts was implemented in Phase 2 (02-10).
    It reads admin ownership from clasp + inserts into student_admin_ownership table.
  </what-built>
  <action>
    PREREQUISITES before running:
    - .clasprc.local.json must be present and authorized (run: clasp login if needed)
    - vercel env pull must have populated web/.env with production DATABASE_URL_UNPOOLED

    Run the following from /Users/kevinhsieh/Developer/Credit Control/Begifted-Ops/:

    1. cd web && vercel env pull .env
       (This populates DATABASE_URL + DATABASE_URL_UNPOOLED from Vercel/Neon production)

    2. cd web && npm run db:seed-admin
       (Runs: tsx scripts/seed-admin-ownership.ts)

    Expected output:
    [seed-admin-ownership] Fetching admin ownership from Apps Script via clasp...
    [seed-admin-ownership] Parsed N ownership entries.
    [seed-admin-ownership] Inserting/updating N rows in student_admin_ownership...
    [seed-admin-ownership] OK — inserted/updated N rows.

    If output says 0 rows or shows an error, DO NOT PROCEED to Gate 4. Investigate.
    The script is idempotent (ON CONFLICT DO UPDATE) — safe to re-run.

    After successful run:
    - Note the N (row count) from the output
    - Document N in the cutover PR description under "Seed evidence: N rows"
    - CRITICAL: Run: cd web && rm .env (or git checkout -- .env if it already existed)
      The .env file must NEVER be committed to git.
  </action>
  <how-to-verify>
    Confirm: npm run db:seed-admin printed "OK — inserted/updated N rows" with N > 0.
    Confirm: .env file is NOT tracked by git: git status | grep ".env" | wc -l (must be 0 or .env shows as gitignored)
  </how-to-verify>
  <resume-signal>
    Type "gate-3-passed N" where N is the row count from the seed output.
    Or describe which step failed.
  </resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Gate 4: Operator manual QA on Vercel preview URL (D-35 item 4)</name>
  <what-built>
    The full Wisenet+Postgres cutover is now deployed to a Vercel preview URL.
  </what-built>
  <action>
    Open the Vercel preview URL and perform the following manual QA walkthrough.
    This verifies the cutover works end-to-end for operators.

    QA STEPS (check each):
    1. Dashboard loads — you see the student queue populated from Wisenet data (not Sheets data)
    2. Filter by admin — select an admin from the filter; queue updates to show only that admin's students
    3. Set action on a student — click a student → Student Detail → set action to "contacted"
       Verify: action saves without error; student detail shows "contacted" state
    4. Bulk action — select 3-5 students → set bulk action to "pending-callback"
       Verify: all selected students update; no errors in browser console
    5. Mark inactive — open a student → mark as inactive
       Verify: student disappears from queue
    6. Open student detail — click any student in queue
       Verify: Student Detail panel opens and shows:
       a. Student name, parent, admin assignment
       b. "View pre-cutover history →" link visible below the parent/admin line
       c. The link has appropriate styling (muted, small text, underline)
    7. Click archive link — click "View pre-cutover history →"
       Verify: Opens Google Sheets URL in new tab (not in same tab)
       Verify: The Sheets URL points to the correct DashboardActionsState tab (or TODO placeholder)
    8. /api/health — open {preview_url}/api/health in browser
       Verify: JSON response with { status: "ok", timestamp, subsystems: { wisenet: { status: "ok" }, postgres: { status: "ok" }, auth: { status: "ok" } } }
       Verify: HTTP 200 status

    ALSO VERIFY:
    - No "HTTP -1" or "chunk-transfer" errors in browser console
    - No "Unauthorized" errors when logged in with a valid Google Workspace account
    - Calendar panel loads (if visible in this preview)

    ROLLBACK READINESS (before proceeding):
    - Go to Vercel Dashboard → Project → Deployments
    - Confirm prior production build (dpl_9dvQvrEe6RB1ZaEE9MN1ooCx2REV) is listed
    - Confirm "Promote Deployment" option is available for the prior build
    - Document this in the PR description: "Rollback: prior build dpl_9dvQvrEe6RB1ZaEE9MN1ooCx2REV reachable in Vercel dashboard"

    CACHE NOTE: After Gate 4, Runtime Cache may serve stale entries for up to 5 minutes (expire: 300s).
    If dashboard data seems stale immediately after deploy, wait up to 5 minutes. This is expected behavior.
  </action>
  <how-to-verify>
    Complete all 8 QA steps above. Note any issues.
    Confirm rollback build is reachable.
  </how-to-verify>
  <resume-signal>
    Type "gate-4-passed" after completing all QA steps with no blocking issues.
    If any issue found, describe what failed.
  </resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Gate 5: Transfer cutover diff to prod-snapshot and deploy (D-42)</name>
  <what-built>
    All Phase 1+2+3 code is committed in Begifted-Ops/.git.
    Begifted-Ops-prod-snapshot/ is the live deploy source linked to kasheesh711/begifted-credit-dashboard.
  </what-built>
  <action>
    Transfer the Phase 3 cutover from Begifted-Ops/web/ to Begifted-Ops-prod-snapshot/web/.

    STEP 1 — Identify the prod-snapshot directory:
    ls /Users/kevinhsieh/Developer/Credit\ Control/Begifted-Ops-prod-snapshot/web/

    STEP 2 — rsync Begifted-Ops/web/ into prod-snapshot/web/ (preserving the prod-snapshot .git):
    cd /Users/kevinhsieh/Developer/Credit\ Control/
    rsync -av --exclude=".git" --exclude=".vercel" --exclude="node_modules" --exclude=".env" --exclude=".env.local" --exclude=".env.production" \
      Begifted-Ops/web/ Begifted-Ops-prod-snapshot/web/

    STEP 3 — Conflict resolution for the 3 files that differ (D-41 / D-42):
    After the rsync, verify these three files contain the Begifted-Ops/ versions:
    - web/src/lib/dashboard/analytics.ts — must have the Phase 2 version
    - web/src/lib/runtime/env.ts — must contain getWisenetEnv() and getDbEnv() (Phase 2 additions)
    - web/src/test/dashboard-logic.test.ts — must have the Wisenet parity block

    The rsync will have copied Begifted-Ops/web/ versions which are CORRECT. Verify with:
    grep "getWisenetEnv\|getDbEnv" Begifted-Ops-prod-snapshot/web/src/lib/runtime/env.ts
    (must return both function names)

    STEP 4 — Review the diff in prod-snapshot:
    cd /Users/kevinhsieh/Developer/Credit\ Control/Begifted-Ops-prod-snapshot/
    git status
    git diff --stat

    Confirm the diff looks like the Phase 3 cutover:
    - next.config.ts changed (cacheComponents: true)
    - service.ts changed (use cache: remote)
    - actions.ts changed (use server facade)
    - Route handlers changed (5 files)
    - lib/sheets/ deleted
    - memory-cache.ts deleted
    - googleapis removed from package.json
    - etc.

    STEP 5 — Commit in prod-snapshot:
    cd /Users/kevinhsieh/Developer/Credit\ Control/Begifted-Ops-prod-snapshot/
    git add -A
    git commit -m "feat: Phase 3 Wisenet+Postgres cutover (transferred from Begifted-Ops/)"

    STEP 6 — Push to kasheesh711/begifted-credit-dashboard main:
    git push origin main

    STEP 7 — Confirm Vercel auto-deploys:
    Go to Vercel Dashboard → Project → Deployments
    The new deployment should appear within 30-60 seconds of the push.
    Wait for deployment to complete (status: "Ready")

    STEP 8 — Set Vercel alias if needed:
    If web-six-liard-22.vercel.app needs to be aliased to the new deployment:
    vercel alias set <new-deployment-url> web-six-liard-22.vercel.app

    STEP 9 — Post-deploy verification:
    Open https://web-six-liard-22.vercel.app (or the production URL)
    Confirm the dashboard loads with real data.
    Check /api/health returns status: "ok" for all subsystems.

    DO NOT MERGE BACK: Begifted-Ops/.git and Begifted-Ops-prod-snapshot/.git are separate repos.
    This is the "work here, transfer at end" model per D-42.
  </action>
  <how-to-verify>
    1. `cd Begifted-Ops-prod-snapshot && git log --oneline -1` shows the new commit
    2. Vercel dashboard shows the deployment as "Ready"
    3. Production URL loads the dashboard with Wisenet data
    4. /api/health returns { status: "ok" } on production URL
  </how-to-verify>
  <resume-signal>
    Type "gate-5-deployed" with the Vercel deployment URL of the new production build.
    Or describe which step failed for debugging.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → prod Neon | Admin seed uses DATABASE_URL_UNPOOLED against production database |
| Begifted-Ops/ → prod-snapshot/ | File transfer copies all web/ files including tests; no secrets should be in web/ source files |
| prod-snapshot/ → Vercel | Git push to kasheesh711/begifted-credit-dashboard triggers production deployment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10-1 | Tampering | admin seed inserts wrong allowlist | mitigate | seed-admin-ownership.ts uses VALID_ADMIN_KEYS allowlist built from ADMIN_OWNER_REGISTRY; fallback JSON written on any failure path (D-21 per STATE.md). Operator documents N (row count) in PR description — 0 rows triggers investigation before gate passes |
| T-03-10-2 | Information Disclosure | .env file accidentally transferred to prod-snapshot via rsync | mitigate | rsync command includes --exclude=".env" --exclude=".env.local" --exclude=".env.production". After transfer, operator runs `git status | grep ".env"` in prod-snapshot to confirm no .env files staged |
| T-03-10-3 | Denial of Service | wrong deployment promoted to production | mitigate | Operator verifies rollback build is reachable BEFORE starting Gate 5. D-27 rollback: Promote Deployment click in Vercel dashboard. SLA: rollback in <30 seconds if dashboard errors >5% within 30 minutes |
</threat_model>

<verification>
Final verification after all gates pass:
1. `cd web && npm test -- --run` exits 0 with 180+ tests (Gate 1 documented)
2. `cd web && npx tsc --noEmit` exits 0 (Gate 1 documented)
3. `cd web && npm run build` exits 0 (Gate 1 documented)
4. `cd web && npm run lint:no-revalidate-max` exits 0 (Gate 1 documented)
5. Admin seed row count > 0 documented in PR description (Gate 3 documented)
6. Manual QA walkthrough completed on Vercel preview (Gate 4 documented)
7. Production URL loads Wisenet-backed dashboard (Gate 5 documented)
8. `curl -s https://web-six-liard-22.vercel.app/api/health | jq .status` returns "ok"
</verification>

<success_criteria>
- All 4 D-35 gate items passed and documented
- Admin-ownership seed row count > 0 documented in PR description
- ARCHIVE_ACTION_SHEET_URL confirmed or TODO acknowledged in PR description
- Begifted-Ops-prod-snapshot/web/ committed with Phase 3 cutover diff
- Pushed to kasheesh711/begifted-credit-dashboard main
- Vercel deployed new production build
- Production URL serves Wisenet+Postgres-backed dashboard
- Rollback reachability confirmed (prior dpl_9dvQvrEe6RB1ZaEE9MN1ooCx2REV accessible)

NOTE: TEST-04 (50-concurrent load test, p95 < 500ms) is DEFERRED to Phase 4 DEPL-04 per D-34.
This is intentional — load-gen tooling not installed; fits Phase 4 deploy hardening work.
REQUIREMENTS.md TEST-04 will be updated to reflect this deferral.
</success_criteria>

<output>
After completion, create `.planning/phases/03-service-cutover/03-10-SUMMARY.md` with:
- Gate 1: test count, build result
- Gate 2: CI run URL or "no new migrations" rationale
- Gate 3: admin seed row count N
- Gate 4: QA walkthrough notes — any issues found
- Gate 5: prod-snapshot commit SHA, Vercel deployment URL, production verification
- ARCHIVE_ACTION_SHEET_URL status (real URL or TODO still in place)
- Rollback readiness confirmation

Also create `.planning/phases/03-service-cutover/03-PHASE-SUMMARY.md` summarizing the full Phase 3 execution:
- Wave structure executed
- All requirements closed: SVC-01..08, TEST-06
- TEST-04 deferred to Phase 4 DEPL-04 per D-34
- Key decisions confirmed (D-38..D-43)
- Production deployment URL
</output>
