---
phase: 03-service-cutover
plan: 10
type: execute
wave: 6
depends_on:
  - 03-09
files_modified: []
requirements: []
autonomous: false
must_haves:
  truths:
    - "D-31 admin seed has been run against production Neon and the row count is documented in the cutover PR description"
    - "RESEARCH §A8 pre-deploy cache tag purge has been performed for the `dashboard-payload` tag in the Vercel dashboard"
    - "D-35 gate 4 manual QA click-through completed against the Vercel preview URL with all 8 steps verified"
    - "All 3 operator gates pasted into chat as resume signals before the cutover PR is merged to main"
  artifacts: []
  key_links:
    - from: "Plan 03-10 (Pre-Merge Gate)"
      to: "Cutover PR merge button"
      via: "operator-only checkpoints"
      pattern: "(operator gate, no automation)"
---

<objective>
Operator-only Pre-Merge Gate that blocks the cutover PR from being merged until three Day-Zero operator actions are confirmed: D-31 admin-ownership seed run against production Neon, RESEARCH §A8 pre-deploy cache-tag purge from the Vercel dashboard, and D-35 gate 4 manual QA click-through on the Vercel preview URL.

Purpose: Plans 03-01 through 03-09 ship the code commits (build green, tests green, lockfile pruned). But three things still cannot land in code: (1) admin seed has to run against production Neon with a real `DATABASE_URL_UNPOOLED` (D-31 — no CI path because clasp auth is local-only); (2) the Vercel cache tag for `dashboard-payload` must be purged BEFORE the new `'use cache: remote'` directive's first read, otherwise the prior `unstable_cache`-tagged Sheets-shaped payload will satisfy reads for up to TTL (RESEARCH §A8 / Assumption A8); (3) Kevin's eyeballs must verify the dashboard, filter, action, bulk action, inactive toggle, student detail, archive link, and `/api/health` all behave on a real preview URL (D-35 gate 4). Without these three confirmations, the cutover ships uncovered against the exact failure modes 03-CONTEXT and 03-RESEARCH cataloged.

Output: No new files, no code edits — three `checkpoint:human-action` checkpoints that each pause execution until Kevin pastes the agreed resume signal back into chat. The orchestrator will not advance to a merge step until all three resume signals are received.

Scope boundary vs. 03-09: Plan 03-09 ends at "build green, tests green, lockfile pruned, dependency-removal commit valid in isolation." Plan 03-10 is the cutover-day operator checklist that gates the merge button. 03-09 is the last code-producing plan; 03-10 adds zero commits. Both are required before main absorbs the cutover.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-service-cutover/03-CONTEXT.md
@.planning/phases/03-service-cutover/03-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Operator runs admin-ownership seed against production Neon (D-31)</name>
  <read_first>
    - .planning/phases/03-service-cutover/03-CONTEXT.md §D-31 (lines 113-124 — full pre-merge operator action description)
    - web/scripts/README-db-ops.md §"First run (Phase 3 cutover)" (per D-31 ripple — Plan 02-10 deliverable)
  </read_first>
  <what-built>
    No code change in this task — Kevin must run the admin-ownership seed against production Neon BEFORE merging the cutover PR. Per D-31, this is intentionally a pre-merge operator action (not a CI-on-merge step) because seed-admin-ownership.ts uses clasp auth which is local-only by security policy, AND because running it post-merge would show every student as `RemainingCredits/Admin = unassigned` until the seed finished — a visible operator outage.
  </what-built>
  <how-to-verify>
    Run, in order, from the project root:

    1. `cd web && vercel env pull .env`
       - This populates `DATABASE_URL_UNPOOLED` against prod Neon (set up via the Vercel ↔ Neon Marketplace integration earlier).
       - Confirm `.env` is gitignored before proceeding (`grep -E "^\.env$" web/.gitignore` should match).
       - Confirm the file is not opened in a tracked diff (`git status web/.env` should not appear; if it does, STOP — `.env` belongs in `.gitignore` per CLAUDE.md §Security).
    2. `cd web && npm run db:seed-admin`
       - Expected output line (per D-31 line 118): `[seed-admin-ownership] Parsed N entries. OK — inserted/updated N rows.`
       - Capture the exact value of N from that line.
    3. Document the row count N in the cutover PR description under a "Seed evidence" section (per D-31 line 119).
       - Acceptable formatting: `**Seed evidence:** \`db:seed-admin\` printed \`Parsed 6 entries. OK — inserted/updated 6 rows.\` on 2026-MM-DD against prod Neon (run by kevhsh7).` (numbers illustrative — substitute the real N).
    4. Re-run safety check (per D-31 line 122): the script's `ON CONFLICT (student_key) DO UPDATE` clause means re-running is idempotent. If you spot an issue and need to re-run, the row count stays stable; no data corruption from a second run.

    Why human-only: clasp auth is local-only per security policy (CLAUDE.md §Apps Script line 19, AGENTS.md §Security). Running this in CI would require adding clasp credentials as a GitHub Actions secret — unnecessary attack surface for a one-time operation. AND running post-merge would display every student as "Admin: Unassigned" until the seed finished, which is an operator-visible outage Kevin has explicitly chosen to avoid (D-31 line 122).
  </how-to-verify>
  <acceptance_criteria>
    Operator pastes the resume signal into chat with the actual N value substituted:

    `Operator confirms: db:seed-admin printed [seed-admin-ownership] Parsed N entries. OK — inserted/updated N rows. with N=___ (documented in cutover PR description).`
  </acceptance_criteria>
  <verify>
    <automated>n/a — operator gate</automated>
  </verify>
  <resume-signal>Operator confirms: db:seed-admin printed [seed-admin-ownership] Parsed N entries. OK — inserted/updated N rows. with N=___ (documented in cutover PR description).</resume-signal>
  <done>
    Admin seed has run against production Neon, the row count N is recorded in the PR description's "Seed evidence" section, and the operator has pasted the resume signal with N filled in.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator purges `dashboard-payload` cache tag from Vercel dashboard (RESEARCH §A8)</name>
  <read_first>
    - .planning/phases/03-service-cutover/03-RESEARCH.md §Assumption A8 (line 1266-1267 — the explicit pre-deploy purge requirement)
    - .planning/phases/03-service-cutover/03-03-PLAN.md §rollback (mentions A8 in passing — this task is its FIRST-CLASS enforcement point)
  </read_first>
  <what-built>
    No code change — Kevin must purge the `dashboard-payload` runtime cache tag from the Vercel dashboard BEFORE the cutover PR's first request lands in production. Per RESEARCH §A8 / Assumption A8 (Pattern 4 + Critical Finding #4 risk path), the `'use cache: remote'` directive's function-id hash includes the function source, so post-cutover service.ts SHOULD get a fresh cache key. But A8 is LOW-confidence — if the assumption fails, the prior `unstable_cache`-tagged payload (which uses the SAME `dashboard-payload` tag string) could satisfy the first post-deploy reads for up to TTL with Sheets-shaped data. That would surface as a payload-shape regression visible to operators. The purge eliminates the risk window.
  </what-built>
  <how-to-verify>
    Steps (per Vercel dashboard UI):

    1. Open the Vercel dashboard for the BeGifted Ops production project (project ID `prj_79Y3JFkRszm28pAltR3cbolYujZp` per CLAUDE.md §Configuration / `web/.vercel/project.json`).
    2. Navigate to Project → Storage → Runtime Cache (or Project → Settings → Cache, depending on Vercel UI version as of 2026-04-29 — look for "Runtime Cache" or "Cache Tags").
    3. Locate the `dashboard-payload` cache tag (or matching entry).
    4. Click the "Purge" / "Invalidate" / "Expire" action for that tag.
    5. Confirm the dashboard shows a "Tag purged" or "Cache cleared for `dashboard-payload`" success message (exact wording depends on Vercel UI).
    6. Capture a screenshot of the success state and attach it to the cutover PR description under a "Pre-deploy cache purge" section (or paste the text confirmation if screenshot capture is impractical).
    7. NOTE: do NOT click "Promote deployment" yet — the purge is a pre-deploy step, not a deploy step. After this task confirms, Tasks 3 (manual QA on preview) runs against the preview URL FIRST, and only then does the merge to main happen, which triggers the actual production deploy.

    Timing constraint: the purge must happen BEFORE the new deployment goes live in production. Acceptable orderings:
    - (preferred) Purge → merge to main → Vercel auto-deploys production with empty `dashboard-payload` tag → first read repopulates cache cleanly.
    - (also acceptable) Purge happens AFTER merge but BEFORE any operator hits the dashboard. Risky because uptime monitors / external pingers may reach `/api/dashboard` before the purge completes.

    Why human-only: Vercel does not expose a programmatic "purge cache tag" endpoint that's documented at the same MEDIUM-confidence tier as the dashboard UI. The `getCache().expireTag(...)` API exists but runs FROM Vercel functions (i.e., AFTER deploy), not BEFORE. The dashboard purge is the only pre-deploy lever Kevin has.
  </how-to-verify>
  <acceptance_criteria>
    Operator pastes the resume signal into chat:

    `Operator confirms: \`dashboard-payload\` cache tag purged from Vercel dashboard at HH:MM Asia/Bangkok on 2026-MM-DD. Pre-deploy purge evidence attached to cutover PR description (screenshot or text confirmation).`
  </acceptance_criteria>
  <verify>
    <automated>n/a — operator gate</automated>
  </verify>
  <resume-signal>Operator confirms: `dashboard-payload` cache tag purged from Vercel dashboard at HH:MM Asia/Bangkok on 2026-MM-DD. Pre-deploy purge evidence attached to cutover PR description.</resume-signal>
  <done>
    The `dashboard-payload` cache tag has been purged from the Vercel runtime cache, evidence is in the PR description, and the operator has pasted the resume signal with timestamp filled in.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Operator manual QA click-through on Vercel preview URL (D-35 gate 4)</name>
  <read_first>
    - .planning/phases/03-service-cutover/03-CONTEXT.md §D-35 (lines 191-201 — full 4-gate merge checklist; this task is gate #4)
    - .planning/phases/03-service-cutover/03-02-PLAN.md §Task 1 (the archive-link affordance Kevin verifies in step 6 below)
  </read_first>
  <what-built>
    No code change — Kevin clicks through the deployed Vercel PR-preview URL and verifies the cutover behaves correctly across the 8-step operator workflow. This is D-35 gate 4 (the fourth and final pre-merge gate; gates 1-3 are automated and proven by Plans 03-01 through 03-09).
  </what-built>
  <how-to-verify>
    Open the Vercel PR-preview URL in a browser (Vercel auto-deploys preview URLs for every PR commit; the URL is in the PR's checks list — typically `https://web-<hash>-begifted-ops-team.vercel.app` or similar). Sign in with a Google account on the `STAFF_ALLOWLIST`. Then perform every step in order:

    1. **Dashboard loads:** confirm the dashboard renders within ~5 seconds of sign-in. Sanity-check that the queue panel shows the expected ~3K active students (or a realistic subset) — NOT an empty list, NOT a loading spinner stuck, NOT a 401/500 toast.
    2. **Filter by admin:** click the "Admin" filter dropdown in the filter toolbar. Select one of the 6 named admins from `ADMIN_OWNER_REGISTRY`. Confirm the queue narrows to that admin's students. Click "Unassigned" to verify the unassigned bucket is also filterable.
    3. **Set action:** click on a student row to expand the action menu (or whatever the existing UI affordance is — refer to `dashboard.html`/`dashboard-shell.tsx` parity). Set the action to "contacted". Confirm the row's status pill updates to reflect the new state. Hard-refresh the page and confirm the state persists (proves Postgres write + revalidateTag round-trip).
    4. **Bulk action:** select multiple students via checkbox or shift-click. Open the bulk-action bar. Set the bulk status to "pending-callback". Confirm all selected students update simultaneously. Hard-refresh and confirm persistence (proves D-25 WebSocket Pool transactional bulk write).
    5. **Mark inactive:** open one student's detail panel. Click "Mark inactive" (or the equivalent affordance). Confirm the student is removed from the active queue. Hard-refresh and confirm the student stays inactive (proves `markInactiveStudent` actions facade method).
    6. **Open student detail → archive link:** open any student's detail panel (any active student). Verify the sticky header renders the "View pre-cutover history →" link (per Plan 03-02 / D-30 / SVC-08). Click it and verify a NEW TAB opens at the URL provided in Plan 03-02 Task 1 (the operator-confirmed `ARCHIVE_ACTION_SHEET_URL`). Confirm the new tab loads the DashboardActionsState Google Sheets tab without error (Workspace SSO may prompt).
    7. **`/api/health` structured response:** in the same browser session, navigate to `<preview-url>/api/health`. Confirm the response is a structured JSON with the D-32 shape:
       ```json
       {
         "status": "ok",
         "timestamp": "2026-MM-DDT...",
         "subsystems": {
           "wisenet": { "status": "ok", "latencyMs": <number> },
           "postgres": { "status": "ok", "latencyMs": <number> },
           "auth": { "status": "ok" }
         },
         "deployedAt": "dpl_..."
       }
       ```
       HTTP status code must be 200 (not 503).
    8. **Sign-out + session boundary:** click the sign-out affordance (or visit `/api/auth/signout`). Confirm the dashboard becomes unreachable without re-auth. Sign in with an account NOT on the STAFF_ALLOWLIST (if Kevin has a test account) and confirm sign-in is rejected — bonus check.

    If ANY of steps 1-7 fail, STOP. Do NOT paste the resume signal. File a gap-closure issue against the failing plan (e.g., step 3 failing → revise Plan 03-04 or 03-05) and re-run the cutover-PR build before retrying. Step 8 is bonus — failure shouldn't block merge but should be noted in the PR.
  </how-to-verify>
  <acceptance_criteria>
    Operator pastes the resume signal into chat with the preview URL substituted:

    `Operator confirms: D-35 gate 4 manual QA click-through completed against Vercel preview URL <preview-url> on 2026-MM-DD. All 8 steps verified (dashboard, filter, action, bulk action, mark inactive, archive link, /api/health, sign-out). No regressions detected.`

    Or, if any step regressed:

    `Operator FAILS: D-35 gate 4 — step <N> failed with <description>. Cutover blocked pending revision of Plan 03-<NN>.`
  </acceptance_criteria>
  <verify>
    <automated>n/a — operator gate</automated>
  </verify>
  <resume-signal>Operator confirms: D-35 gate 4 manual QA click-through completed against Vercel preview URL <preview-url> on 2026-MM-DD. All 8 steps verified (dashboard, filter, action, bulk action, mark inactive, archive link, /api/health, sign-out). No regressions detected.</resume-signal>
  <done>
    All 8 manual QA steps have been verified against the Vercel preview URL, no regressions detected, and the operator has pasted the resume signal with the preview URL filled in.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator local machine → Vercel CLI (`vercel env pull`) | Pulls prod env vars to a local file; trusted boundary because clasp + Vercel auth are local-only per CLAUDE.md §Security. `.env` is gitignored. |
| Operator local machine → production Neon (`db:seed-admin`) | Direct write to prod via `DATABASE_URL_UNPOOLED`. Idempotent on `student_key` per D-31 line 122. |
| Operator browser → Vercel dashboard (cache purge) | Trusted Workspace SSO; manual UI action; no automation surface. |
| Operator browser → Vercel preview URL | Same NextAuth gate as production; STAFF_ALLOWLIST enforced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-40 | Tampering | `vercel env pull` overwrites a customized local `.env` | mitigate | The "how-to-verify" step explicitly tells the operator to re-confirm `.env` is gitignored and not in `git status` after pulling. If a local `.env` was customized for development, operator should back it up first (`cp web/.env web/.env.backup`) before running `vercel env pull`. |
| T-03-41 | Spoofing | Operator runs `db:seed-admin` against the wrong DB | mitigate | `vercel env pull` populates `DATABASE_URL_UNPOOLED` from the Vercel-Neon Marketplace integration — the value is the Vercel-managed production URL by definition. Operator cannot easily target a wrong DB without manually editing `.env` after pulling, and the script logs the row count which is sanity-checkable (~6 admins for Phase 3). |
| T-03-42 | Repudiation | Cache purge claimed but not actually performed | mitigate | Resume signal requires evidence (screenshot or text confirmation in the PR description). Future incident reviews can audit the PR for the purge evidence. If the purge was skipped and stale Sheets-shaped data leaks post-deploy, the PR description carries the audit trail. |
| T-03-43 | Information Disclosure | Manual QA click-through against a preview URL exposes prod-shaped data to anyone watching the operator's screen | accept | Kevin operates from a private workstation per security policy. Preview URLs are NextAuth-gated to STAFF_ALLOWLIST same as production. No anonymous access. |
| T-03-44 | Denial of Service | Operator skips a gate to rush the merge | mitigate | Three resume signals are explicit and structured. The orchestrator will not advance to the merge step without all three. Pre-merge tooling can verify the signals appear in the PR thread before allowing merge (Phase 4 may automate this; Phase 3 relies on operator discipline + checkpoint structure). |
</threat_model>

<verification>
This plan is operator-only — no automated verification. The three resume signals ARE the verification artifacts:

- D-31 admin seed run + N documented (Task 1 resume signal)
- A8 cache tag purged (Task 2 resume signal)
- D-35 gate 4 manual QA click-through verified (Task 3 resume signal)

Cross-references the verification scope of Plans 03-01 through 03-09:
- 03-01..03-09: code-side verification (build, tests, lockfile)
- 03-10 (this plan): operator-side verification (seed, purge, manual QA)
- Both halves required before main absorbs the cutover.
</verification>

<success_criteria>
- Maps to ROADMAP Phase 3 SC#1-#5 collectively — without these gates, the cutover ships uncovered against the exact failure modes 03-CONTEXT and 03-RESEARCH cataloged (stale-cache visibility, missing admin attribution, regression on operator workflows).
- Closes D-31 (admin seed pre-merge requirement), D-35 gate 4 (manual QA), and RESEARCH §A8 (pre-deploy cache purge) in a single gating plan instead of leaving them implicit in earlier plans' rollback sections.
- Plan 03-10 adds NO commits; it gates the cutover-PR merge button. Phase 3 still ships in 9 code commits per D-33 ordering.
</success_criteria>

<rollback>
This plan produces no commits. Rollback is a no-op for the repo state.

Per-task rollback semantics:
- **Task 1 (admin seed):** the seed insert is idempotent on `student_key` (per D-31 line 122). If the cutover-PR is reverted post-merge via Vercel-dashboard rollback, the admin-ownership rows in production Neon stay populated. The pre-cutover Vercel deploy doesn't read from `student_admin_ownership` (Phase 1 / Phase 2-era code path), so the populated table is harmless. Forward-fix when re-attempting cutover: re-running `db:seed-admin` is safe (row counts stable).
- **Task 2 (cache purge):** purging the tag is idempotent. If cutover is rolled back via Vercel dashboard, the prior deploy's `unstable_cache` repopulates `dashboard-payload` naturally on the next mutation. No corrective action needed.
- **Task 3 (manual QA):** if QA detected a regression, the cutover PR is blocked and Plans 03-01 through 03-09 are revised via gap-closure mode before re-attempting. The QA result IS the rollback signal.
</rollback>

<output>
After completion, create `.planning/phases/03-service-cutover/03-10-SUMMARY.md` capturing:
- The three resume signals as pasted by the operator (with N, timestamps, preview URL filled in)
- Any anomalies surfaced during seed run / cache purge / manual QA (e.g., npm install printed an unexpected warning, dashboard load latency was higher than expected, /api/health subsystem latency was unusual)
- Confirmation that the cutover PR was merged on 2026-MM-DD at HH:MM Asia/Bangkok with all three pre-merge gates green
- Cross-reference to Phase 4 DEPL-04 (Sentry / Playwright / X-BG-Deploy-Id) for the post-cutover hardening that supersedes the manual gate-4 QA in Phase 4 onwards
</output>
