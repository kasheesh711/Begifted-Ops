---
phase: 03-service-cutover
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/next.config.ts
requirements:
  - SVC-01
autonomous: true
must_haves:
  truths:
    - "Next.js production build succeeds with cacheComponents enabled"
    - "Next.js dev server starts without cacheComponents-related warnings"
  artifacts:
    - path: "web/next.config.ts"
      provides: "Top-level cacheComponents: true sibling key"
      contains: "cacheComponents: true"
  key_links:
    - from: "web/next.config.ts"
      to: "Next.js 16 build pipeline"
      via: "NextConfig.cacheComponents flag"
      pattern: "cacheComponents:[[:space:]]*true"
---

<objective>
Enable Next.js 16 Cache Components support by adding `cacheComponents: true` to `web/next.config.ts` (SVC-01). This is the smallest, lowest-risk Phase 3 commit and unblocks the `'use cache: remote'` directive consumed by Plan 03-03 (service.ts rewrite).

Purpose: Cache Components is a build-time directive that activates the Next.js 16 caching primitives (`use cache: remote`, `cacheTag`, `cacheLife`). Without it, Plan 03-03's `getDashboardPayload` directive is a no-op.
Output: Single-line `cacheComponents: true` sibling key inside the existing `NextConfig` literal, plus a green production build proving compatibility.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-service-cutover/03-CONTEXT.md
@.planning/phases/03-service-cutover/03-RESEARCH.md
@.planning/phases/03-service-cutover/03-PATTERNS.md
@web/next.config.ts

<interfaces>
<!-- Current shape of web/next.config.ts (full file) -->
```ts
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: true,
};

export default nextConfig;
```

The `NextConfig` type already exposes `cacheComponents` in Next.js 16.0+ — no `@ts-expect-error` needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable cacheComponents in next.config.ts</name>
  <read_first>
    - web/next.config.ts (full file, 7 lines — read in one pass)
    - .planning/phases/03-service-cutover/03-PATTERNS.md §`web/next.config.ts` (verifies sibling-key placement)
    - .planning/phases/03-service-cutover/03-CONTEXT.md §code_context line 337 ("verify this is a sibling key, not nested")
  </read_first>
  <files>web/next.config.ts</files>
  <action>
    Add `cacheComponents: true` as a sibling top-level property of `NextConfig` (NOT nested inside any `experimental`/`compiler`/etc. namespace) per D-23 (Phase 3 enables `cacheComponents`) and Pattern Map §next.config.ts.

    Final exact shape:

    ```ts
    import type { NextConfig } from "next";

    const nextConfig: NextConfig = {
      reactStrictMode: true,
      cacheComponents: true,    // SVC-01 — Phase 3 enables Cache Components
    };

    export default nextConfig;
    ```

    Do NOT introduce any other config properties. Do NOT add an `experimental` namespace. Do NOT add comments other than the trailing inline tag shown above.

    After the edit, run `cd web && npx tsc --noEmit` to confirm `NextConfig` type accepts `cacheComponents` (Next.js 16.2.1 ships this in the public type — RESEARCH §Pattern 1).

    Then run `cd web && npm run build` to confirm the production build succeeds. This is the empirical compatibility check (per VALIDATION.md "Manual-Only Verifications" line 88 / Research Assumption A3).
  </action>
  <acceptance_criteria>
    - `grep -n "cacheComponents:[[:space:]]*true" web/next.config.ts` returns exactly 1 match.
    - `grep -n "experimental" web/next.config.ts` returns 0 matches (the flag is top-level in Next.js 16, not under `experimental`).
    - `cd web && npx tsc --noEmit` exits 0 with no errors related to `next.config.ts`.
    - `cd web && npm run build` exits 0; output contains "✓ Compiled successfully" and the existing 164-test baseline test suite still passes when run.
    - `web/next.config.ts` total line count is 8 (was 7) — proving exactly one new line was added.
  </acceptance_criteria>
  <verify>
    <automated>cd "/Users/kevinhsieh/Developer/Credit Control/Begifted-Ops/web" && grep -n "cacheComponents:[[:space:]]*true" next.config.ts && npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>
    `cacheComponents: true` lives as a sibling top-level key in `NextConfig`; tsc clean; production build green; no `experimental` wrapper introduced.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build-time → runtime | `next.config.ts` is parsed at `next build`; misconfig surfaces at deploy time, not request time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `next.config.ts` flag drift | mitigate | Single-line sibling key with inline comment referencing SVC-01 — reviewers spot drift in PR diff. |
| T-03-02 | Denial of Service | Build failure from incompatible `cacheComponents` × NextAuth 5 beta combo (Research A3 LOW-confidence assumption) | mitigate | Mandatory `npm run build` in acceptance criteria proves compatibility before merge. If build fails, plan is revised before later waves run. |
| T-03-03 | Information Disclosure | Config file leakage | accept | `next.config.ts` is checked-in source; no secrets, no env-derived values; safe to be public. |
</threat_model>

<verification>
- `cd web && npx tsc --noEmit` exits 0
- `cd web && npm run build` exits 0
- `cd web && npm test -- --run` baseline 164/164 still green (Phase 2 baseline preserved)
- `cd web && npm run lint:no-revalidate-max` exits 0 (Phase 2 lint baseline preserved — this plan does not touch `service.ts`)
</verification>

<success_criteria>
- Maps to ROADMAP Phase 3 SC#1 (route-handler payload composition) by enabling the `'use cache: remote'` directive that Plan 03-03 will apply to `service.ts::getDashboardPayload`.
- Single-commit, single-file change leaves the repo in a buildable state per D-33 commit-1 ordering.
</success_criteria>

<rollback>
Per D-27, the rollback path is the Vercel dashboard: an operator clicks "Promote Deployment" on the prior production build (the pre-merge deploy still exists in Deployments tab) and traffic flips back in <30s. For this plan specifically, a `git revert` of the single-line edit also fully undoes it without affecting any other Phase 3 work.
</rollback>

<output>
After completion, create `.planning/phases/03-service-cutover/03-01-SUMMARY.md` capturing:
- Confirmed `NextConfig` type accepted `cacheComponents` without `@ts-expect-error`
- Build duration before vs. after (informational; expected to be unchanged)
- Any warnings emitted during build that mention `cacheComponents`
</output>
