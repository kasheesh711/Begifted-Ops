---
phase: 03-service-cutover
plan: "01"
type: execute
wave: 1
depends_on: ["03-00"]
files_modified:
  - web/next.config.ts
autonomous: true
requirements:
  - SVC-01

must_haves:
  truths:
    - "`cacheComponents: true` is present in web/next.config.ts as a sibling key to reactStrictMode"
    - "npm run build succeeds with cacheComponents enabled"
  artifacts:
    - path: "web/next.config.ts"
      provides: "cacheComponents flag enabling Next.js 16 Cache Components directive"
      contains: "cacheComponents: true"
  key_links:
    - from: "web/next.config.ts"
      to: "web/src/lib/dashboard/service.ts"
      via: "cacheComponents: true enables 'use cache: remote' directive compilation"
      pattern: "cacheComponents.*true"
---

<objective>
Enable `cacheComponents: true` in `web/next.config.ts`. This is the build-time flag that unlocks the `'use cache: remote'` directive, `cacheTag`, `cacheLife`, and `revalidateTag`-with-profile APIs in Next.js 16. Without this flag, the SVC-02 service.ts rewrite in Wave 2 will fail to compile.

Purpose: One-line config change; standalone commit per D-33 commit structure.

Output: `web/next.config.ts` with `cacheComponents: true` alongside `reactStrictMode: true`.
</objective>

<execution_context>
@/Users/kevinhsieh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kevinhsieh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kevinhsieh/Developer/Credit Control/Begifted-Ops/.planning/ROADMAP.md
@/Users/kevinhsieh/Developer/Credit Control/Begifted-Ops/.planning/phases/03-service-cutover/03-CONTEXT.md

Current web/next.config.ts shape (from 03-PATTERNS.md):
```ts
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: true,
};

export default nextConfig;
```

Target shape per 03-PATTERNS.md §next.config.ts:
```ts
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: true,
  cacheComponents: true,    // SVC-01 — Phase 3 enables Cache Components
};

export default nextConfig;
```

Key facts:
- `cacheComponents` is a SIBLING key to `reactStrictMode`, NOT nested inside anything
- `NextConfig` type already exposes `cacheComponents` in Next.js 16.0+ — no `@ts-expect-error` needed
- Project is on Next.js 16.2.1 which fully supports this flag (RESEARCH confirmed)
- Side effects documented in RESEARCH Pattern 2: auth-gated protected routes unaffected; build may error on fetch-without-cache pages — catch via `npm run build` in acceptance criteria
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add cacheComponents: true to next.config.ts</name>
  <files>web/next.config.ts</files>
  <read_first>
    1. web/next.config.ts — read current contents first (verify the 7-line shape matches expectation above)
  </read_first>
  <action>
    Read web/next.config.ts. It currently has only `reactStrictMode: true` inside the nextConfig object.

    Add `cacheComponents: true` as a sibling key immediately after `reactStrictMode: true`. The final
    file must be:

    ```ts
    import type { NextConfig } from "next";

    const nextConfig: NextConfig = {
      reactStrictMode: true,
      cacheComponents: true,    // SVC-01 — Phase 3 enables Cache Components
    };

    export default nextConfig;
    ```

    Then:
    1. Run: cd web && npx tsc --noEmit
       (must exit 0 — TypeScript must accept cacheComponents without errors)
    2. Run: cd web && npm run build
       (must exit 0 — catches any cacheComponents runtime compatibility issues)
    3. Commit:
       git add web/next.config.ts
       git commit -m "feat(03-01): enable cacheComponents in next.config.ts"
  </action>
  <verify>
    <automated>
      grep -n "cacheComponents: true" web/next.config.ts
      cd web && npx tsc --noEmit && echo "tsc: OK"
      cd web && npm run build && echo "build: OK"
    </automated>
  </verify>
  <done>
    grep finds "cacheComponents: true" on a line after "reactStrictMode: true". tsc --noEmit exits 0.
    npm run build exits 0. One-file commit landed on main.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build config → runtime | cacheComponents: true changes Next.js compilation behavior; must verify via npm run build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-1 | Denial of Service | next.config.ts cacheComponents side effects | accept | Risk is build-time prerendering changes for fetch-without-cache pages; mitigated by `npm run build` passing in acceptance criteria. Auth-gated `(protected)/dashboard/page.tsx` is dynamic anyway. LOW severity — caught immediately at build step |
</threat_model>

<verification>
1. `grep "cacheComponents: true" web/next.config.ts` exits 0 and prints the line
2. `cd web && npx tsc --noEmit` exits 0
3. `cd web && npm run build` exits 0
4. `git show HEAD --name-only` shows only `web/next.config.ts` in the commit
5. `git show HEAD --stat` shows 1 file changed, 1 insertion
</verification>

<success_criteria>
- `web/next.config.ts` contains `cacheComponents: true` as a sibling key to `reactStrictMode: true`
- TypeScript compilation exits clean
- Production build succeeds — no new prerender errors from enabling cacheComponents
- Commit message: `feat(03-01): enable cacheComponents in next.config.ts`
</success_criteria>

<output>
After completion, create `.planning/phases/03-service-cutover/03-01-SUMMARY.md` with:
- Confirmation that cacheComponents: true was added
- Result of `npm run build` (any new warnings or errors noted)
- Commit SHA
</output>
