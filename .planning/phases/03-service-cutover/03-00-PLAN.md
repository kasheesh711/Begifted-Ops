---
phase: 03-service-cutover
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - web/src/app/api/dashboard/route.ts
  - web/src/app/api/health/route.ts
  - web/src/app/api/inactive/route.ts
  - web/src/app/api/auth/[...nextauth]/route.ts
  - web/src/app/api/actions/bulk/route.ts
  - web/src/app/api/actions/history/route.ts
  - web/src/app/signin/page.tsx
  - web/src/lib/wisenet/client.ts
  - web/src/lib/wisenet/endpoints.ts
  - web/src/lib/wisenet/types.ts
  - web/src/lib/wisenet/mappers.ts
  - web/src/lib/wisenet/retry.ts
  - web/src/lib/db/schema.ts
  - web/src/lib/db/client.ts
  - web/src/lib/db/bulk-client.ts
  - web/src/lib/db/queries.ts
  - web/src/lib/db/bulk-queries.ts
  - web/drizzle/0000_initial.sql
  - .github/workflows/db-migrate.yml
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/config.json
autonomous: true
requirements: []

must_haves:
  truths:
    - "Seven Sheets-era surface files are present in web/src/app/ and committed"
    - "All Phase 1+2 source files under web/ are committed to git"
    - "All untracked .planning/ artifacts are committed to git"
    - "No secrets (.env, .env.local) are committed"
    - "npm run build passes after all three sub-task commits"
  artifacts:
    - path: "web/src/app/api/dashboard/route.ts"
      provides: "Sheets-era GET dashboard route handler (baseline for SVC-04 swap)"
    - path: "web/src/app/api/health/route.ts"
      provides: "Sheets-era health route handler (baseline for SVC-07 replacement)"
    - path: "web/src/app/api/auth/[...nextauth]/route.ts"
      provides: "NextAuth route handlers restored to app"
    - path: "web/src/lib/wisenet/client.ts"
      provides: "Phase 2 Wisenet HTTP client (Phase 3 consumes)"
    - path: "web/src/lib/db/queries.ts"
      provides: "Phase 2 Drizzle typed queries (Phase 3 consumes)"
    - path: ".planning/REQUIREMENTS.md"
      provides: "SVC-01..08 + TEST-06 requirement IDs (planning artifact)"
  key_links:
    - from: "Begifted-Ops-prod-snapshot/web/src/app/"
      to: "Begifted-Ops/web/src/app/"
      via: "verbatim file copy (cp)"
      pattern: "7 specific files — DO NOT copy analytics.ts, env.ts, dashboard-logic.test.ts"
---

<objective>
Baseline reconciliation — establishes a self-consistent git state in the Begifted-Ops repo before Phase 3 cutover work begins. Three sequential sub-tasks, each an atomic commit.

Purpose: The Begifted-Ops repo currently has `web/` entirely untracked and is missing 7 route handler / page files. This plan brings the working tree into git so all subsequent Phase 3 plans operate against a committed baseline.

Output:
- Sub-task 1 commit: 7 Sheets-era files restored from prod-snapshot
- Sub-task 2 commit: Phase 1+2 source code committed (wisenet client, db layer, tests, scripts, CI workflow)
- Sub-task 3 commit: .planning/ artifacts committed (PROJECT.md, REQUIREMENTS.md, config.json, codebase/, research/, phase 1+2 summaries, phase 3 context files)

**Commit count update:** D-33 originally targeted ~8 commits (pre-rebaseline). With Plan 03-00's 3 sub-task commits added (per D-40) and 03-08 split into 2 commits, the actual sequence is now 13 commits. Additive-then-deletion ordering preserved.

**Note on requirements frontmatter:** Plan 03-00 is a baseline restoration plan. It copies Sheets-era route handler files verbatim and commits uncommitted Phase 1+2 source — it does NOT implement any SVC-* or TEST-* requirement. The actual implementations happen in later plans (SVC-04 in 03-05, SVC-07 in 03-06). requirements: [] is correct for this plan.
</objective>

<execution_context>
@/Users/kevinhsieh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kevinhsieh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kevinhsieh/Developer/Credit Control/Begifted-Ops/.planning/PROJECT.md
@/Users/kevinhsieh/Developer/Credit Control/Begifted-Ops/.planning/ROADMAP.md
@/Users/kevinhsieh/Developer/Credit Control/Begifted-Ops/.planning/STATE.md
@/Users/kevinhsieh/Developer/Credit Control/Begifted-Ops/.planning/phases/03-service-cutover/03-CONTEXT.md

CRITICAL: This plan runs as a SINGLE SEQUENTIAL AGENT. No parallel execution, no worktrees.
The three sub-tasks share the working tree and must execute in order: 1 → 2 → 3.

IMPORTANT TOPOLOGY:
- Begifted-Ops/.git = the repo being committed to
- Begifted-Ops/web/ = active dev tree with all Phase 1+2 files (untracked)
- ../Begifted-Ops-prod-snapshot/web/ = source for the 7 route/page files to restore (Sheets-era baseline)

SAFETY RULES:
- DO NOT copy analytics.ts, env.ts, or dashboard-logic.test.ts from prod-snapshot (D-41)
  Those files exist in Begifted-Ops/web/ with Phase 2 work that must not be clobbered.
- DO NOT add any .env, .env.local, .env.production, or .clasprc.local.json to any commit (D-43, CLAUDE.md security)
- DO NOT add V1 dirty .gs/.html/.json files at repo root to any commit (D-43)
</context>

<tasks>

<task type="auto">
  <name>Sub-task 1: Restore Sheets-era route handlers from prod-snapshot</name>
  <files>
    web/src/app/api/dashboard/route.ts
    web/src/app/api/health/route.ts
    web/src/app/api/inactive/route.ts
    web/src/app/api/auth/[...nextauth]/route.ts
    web/src/app/api/actions/bulk/route.ts
    web/src/app/api/actions/history/route.ts
    web/src/app/signin/page.tsx
  </files>
  <read_first>
    1. /Users/kevinhsieh/Developer/Credit Control/Begifted-Ops-prod-snapshot/web/src/app/api/dashboard/route.ts
    2. /Users/kevinhsieh/Developer/Credit Control/Begifted-Ops-prod-snapshot/web/src/app/api/health/route.ts
    3. /Users/kevinhsieh/Developer/Credit Control/Begifted-Ops-prod-snapshot/web/src/app/api/inactive/route.ts
    4. /Users/kevinhsieh/Developer/Credit Control/Begifted-Ops-prod-snapshot/web/src/app/api/auth/[...nextauth]/route.ts
    5. /Users/kevinhsieh/Developer/Credit Control/Begifted-Ops-prod-snapshot/web/src/app/api/actions/bulk/route.ts
    6. /Users/kevinhsieh/Developer/Credit Control/Begifted-Ops-prod-snapshot/web/src/app/api/actions/history/route.ts
    7. /Users/kevinhsieh/Developer/Credit Control/Begifted-Ops-prod-snapshot/web/src/app/signin/page.tsx
    Verify that analytics.ts, env.ts, and dashboard-logic.test.ts are NOT in the source paths listed above
    before copying anything.
  </read_first>
  <action>
    1. Create destination directories if they do not exist:
       mkdir -p "web/src/app/api/dashboard"
       mkdir -p "web/src/app/api/health"
       mkdir -p "web/src/app/api/inactive"
       mkdir -p "web/src/app/api/auth/[...nextauth]"
       mkdir -p "web/src/app/api/actions/bulk"
       mkdir -p "web/src/app/api/actions/history"
       mkdir -p "web/src/app/signin"

    2. VERBATIM copy each file (do NOT modify contents):
       cp "../Begifted-Ops-prod-snapshot/web/src/app/api/dashboard/route.ts"         "web/src/app/api/dashboard/route.ts"
       cp "../Begifted-Ops-prod-snapshot/web/src/app/api/health/route.ts"             "web/src/app/api/health/route.ts"
       cp "../Begifted-Ops-prod-snapshot/web/src/app/api/inactive/route.ts"           "web/src/app/api/inactive/route.ts"
       cp "../Begifted-Ops-prod-snapshot/web/src/app/api/auth/[...nextauth]/route.ts" "web/src/app/api/auth/[...nextauth]/route.ts"
       cp "../Begifted-Ops-prod-snapshot/web/src/app/api/actions/bulk/route.ts"       "web/src/app/api/actions/bulk/route.ts"
       cp "../Begifted-Ops-prod-snapshot/web/src/app/api/actions/history/route.ts"    "web/src/app/api/actions/history/route.ts"
       cp "../Begifted-Ops-prod-snapshot/web/src/app/signin/page.tsx"                "web/src/app/signin/page.tsx"

    3. Verify the copy did NOT overwrite analytics.ts or env.ts:
       grep -l "getWisenetEnv\|getDbEnv" "web/src/lib/runtime/env.ts" 2>/dev/null
       (must still return env.ts — if blank, something went wrong, do not continue)

    4. Stage ALL 7 files using explicit git add for each:
       git add web/src/app/api/dashboard/route.ts
       git add web/src/app/api/health/route.ts
       git add web/src/app/api/inactive/route.ts
       git add "web/src/app/api/auth/[...nextauth]/route.ts"
       git add web/src/app/api/actions/bulk/route.ts
       git add web/src/app/api/actions/history/route.ts
       git add web/src/app/signin/page.tsx

    5. Commit:
       git commit -m "chore(03-00): restore Sheets-era route handlers from prod-snapshot baseline"

    FORBIDDEN: Do NOT git add any .env file, .clasprc.local.json, or files outside web/src/app/ in this step.
  </action>
  <verify>
    <automated>
      git show --stat HEAD | grep "7 files changed"
      git show HEAD --name-only | grep -E "api/dashboard/route|api/health/route|api/inactive/route|auth/\[\.\.\.nextauth\]/route|actions/bulk/route|actions/history/route|signin/page"
      # Confirm analytics.ts NOT in commit:
      git show HEAD --name-only | grep -v analytics.ts | wc -l
    </automated>
  </verify>
  <done>
    All 7 files appear in HEAD commit. No analytics.ts, env.ts, or dashboard-logic.test.ts in the commit.
    git show --stat HEAD shows exactly 7 files changed.
  </done>
</task>

<task type="auto">
  <name>Sub-task 2: Commit Phase 1+2 source files currently untracked under web/</name>
  <files>
    web/src/lib/wisenet/
    web/src/lib/db/
    web/src/test/
    web/scripts/
    web/drizzle/
    .github/workflows/db-migrate.yml
  </files>
  <read_first>
    Run: git status --short | grep "^??" | grep "web/" | head -60
    This lists untracked files under web/. Review before staging to confirm no .env* files appear.
    Also check: cat web/.gitignore to confirm .env, .env.local, .env.production are listed.
  </read_first>
  <action>
    1. Verify .env files are gitignored (safety check before staging):
       cat web/.gitignore | grep ".env"
       (must show ".env" and ".env.*" or ".env.local" — if missing, DO NOT PROCEED; add them first)

    2. Stage Phase 1+2 source directories. Use git add with explicit paths to avoid picking up gitignored files:
       git add web/src/lib/wisenet/
       git add web/src/lib/db/
       git add web/src/lib/runtime/env.ts
       git add web/src/lib/dashboard/
       git add web/src/lib/auth/
       git add web/src/lib/cache/
       git add web/src/lib/sheets/
       git add web/src/components/
       git add web/src/hooks/
       git add web/src/types/
       git add web/src/app/
       git add web/src/test/wisenet-client.test.ts 2>/dev/null || true
       git add web/src/test/wisenet-mapper.test.ts 2>/dev/null || true
       git add web/src/test/db-queries.test.ts 2>/dev/null || true
       git add web/src/test/db-bulk-queries.test.ts 2>/dev/null || true
       git add web/src/test/calendar.test.ts 2>/dev/null || true
       git add web/src/test/lint-no-revalidate-max.test.ts 2>/dev/null || true
       git add web/src/test/dashboard-logic.test.ts 2>/dev/null || true
       git add web/src/test/actions-route.test.ts 2>/dev/null || true
       git add "web/src/test/helpers/" 2>/dev/null || true
       git add "web/src/test/fixtures/" 2>/dev/null || true
       git add web/scripts/db-migrate.ts 2>/dev/null || true
       git add web/scripts/seed-admin-ownership.ts 2>/dev/null || true
       git add web/scripts/lint-no-revalidate-max.sh 2>/dev/null || true
       git add web/scripts/README-db-ops.md 2>/dev/null || true
       git add web/scripts/compare-live.ts 2>/dev/null || true
       git add web/scripts/ensure-action-sheets.ts 2>/dev/null || true
       git add web/scripts/ensure-inactive-sheet.ts 2>/dev/null || true
       git add web/drizzle/ 2>/dev/null || true
       git add web/package.json 2>/dev/null || true
       git add web/package-lock.json 2>/dev/null || true
       git add web/tsconfig.json 2>/dev/null || true
       git add web/vitest.config.ts 2>/dev/null || true
       git add web/next.config.ts 2>/dev/null || true
       git add web/next-env.d.ts 2>/dev/null || true
       git add web/drizzle.config.ts 2>/dev/null || true
       git add .github/workflows/db-migrate.yml 2>/dev/null || true

    3. Safety audit — confirm no .env files staged:
       git diff --staged --name-only | grep -i ".env"
       (must return empty — if any .env file appears, git restore --staged <file> before committing)

    4. Review staged file count:
       git diff --staged --name-only | wc -l

    5. Commit:
       git commit -m "feat(02-retroactive): commit Phase 1+2 source — wisenet client, drizzle schema/queries, tests, scripts, CI"

    Note: If there are many changed files across discrete areas (e.g. wisenet client vs db layer vs tests),
    the executor may split into multiple commits at its discretion (Claude's Discretion per D-40).
    Suggested split if preferred:
      Commit A: web/src/lib/wisenet/ + web/src/test/wisenet-* + web/src/test/fixtures/wisenet/
      Commit B: web/src/lib/db/ + web/drizzle/ + web/src/test/db-* + .github/workflows/db-migrate.yml
      Commit C: remaining web/ files (dashboard, components, hooks, types, config files, scripts, test files)
  </action>
  <verify>
    <automated>
      git log --oneline -1 | grep "02-retroactive"
      git show HEAD --name-only | grep "web/src/lib/wisenet/client.ts"
      git show HEAD --name-only | grep "web/src/lib/db/schema.ts"
      # Confirm no .env file committed:
      git log --all --full-history -- "web/.env" | wc -l
      # (must be 0 — env file must never appear in git log)
    </automated>
  </verify>
  <done>
    Phase 1+2 source files are committed. git status shows no untracked files under web/
    (except gitignored .env*, .vercel/, node_modules/). No .env file appears in git log.
  </done>
</task>

<task type="auto">
  <name>Sub-task 3: Commit untracked .planning/ artifacts</name>
  <files>
    .planning/PROJECT.md
    .planning/REQUIREMENTS.md
    .planning/config.json
    .planning/codebase/
    .planning/research/
    .planning/phases/01-wisenet-discovery/
    .planning/phases/02-data-layer/
    .planning/phases/03-service-cutover/03-CONTEXT.md
    .planning/phases/03-service-cutover/03-DISCUSSION-LOG.md
    .planning/phases/03-service-cutover/03-RESEARCH.md
    .planning/phases/03-service-cutover/03-PATTERNS.md
    .planning/phases/03-service-cutover/03-VALIDATION.md
  </files>
  <read_first>
    Run: git status --short | grep "^??" | grep ".planning/" | head -40
    Review what .planning/ files are untracked. Also check for gitignored files:
    cat .gitignore | grep planning (may not exist — .planning/ may not be gitignored at repo root level)
    Verify .planning/research/wisenet-postman.json is gitignored or does not exist (must NOT be committed).
  </read_first>
  <action>
    1. Check if wisenet-postman.json is gitignored:
       git check-ignore -v .planning/research/wisenet-postman.json 2>&1
       (if NOT gitignored and the file exists, add to .gitignore before proceeding)

    2. Stage .planning/ artifacts selectively:
       git add .planning/PROJECT.md 2>/dev/null || true
       git add .planning/REQUIREMENTS.md 2>/dev/null || true
       git add .planning/config.json 2>/dev/null || true
       git add .planning/codebase/ 2>/dev/null || true
       git add .planning/research/SUMMARY.md 2>/dev/null || true
       git add .planning/research/STACK.md 2>/dev/null || true
       git add .planning/research/FEATURES.md 2>/dev/null || true
       git add .planning/research/ARCHITECTURE.md 2>/dev/null || true
       git add .planning/research/PITFALLS.md 2>/dev/null || true
       git add .planning/research/WISENET_FIELD_MAP.md 2>/dev/null || true
       git add .planning/research/WISENET_ENDPOINTS.md 2>/dev/null || true
       git add .planning/research/fixtures/ 2>/dev/null || true
       git add .planning/phases/01-wisenet-discovery/ 2>/dev/null || true
       git add .planning/phases/02-data-layer/ 2>/dev/null || true
       git add .planning/phases/03-service-cutover/03-CONTEXT.md 2>/dev/null || true
       git add .planning/phases/03-service-cutover/03-DISCUSSION-LOG.md 2>/dev/null || true
       git add .planning/phases/03-service-cutover/03-RESEARCH.md 2>/dev/null || true
       git add .planning/phases/03-service-cutover/03-PATTERNS.md 2>/dev/null || true
       git add .planning/phases/03-service-cutover/03-VALIDATION.md 2>/dev/null || true

    3. Explicitly exclude wisenet-postman.json if staged by accident:
       git restore --staged .planning/research/wisenet-postman.json 2>/dev/null || true

    4. Safety check for any credential-looking content:
       git diff --staged --name-only | grep -i "postman\|secret\|key\|credential" || echo "No credential files staged"

    5. Commit:
       git commit -m "docs(planning): commit asymmetric .planning/ state"

    6. After all three sub-task commits are done, run the build to confirm the restored baseline compiles:
       cd web && npm run build > /tmp/build.log 2>&1 && echo "build: OK" || (cat /tmp/build.log; exit 1)
  </action>
  <verify>
    <automated>
      git log --oneline -1 | grep "planning"
      git show HEAD --name-only | grep ".planning/REQUIREMENTS.md"
      git show HEAD --name-only | grep ".planning/phases/01-wisenet-discovery"
      # wisenet-postman.json must NOT appear in commit:
      git show HEAD --name-only | grep "wisenet-postman.json" | wc -l
      # (must be 0)
      # Build verification (run after all 3 sub-tasks committed):
      cd web && npm run build > /tmp/build.log 2>&1 && echo "build: OK" || (cat /tmp/build.log; exit 1)
    </automated>
  </verify>
  <done>
    .planning/ artifacts committed. REQUIREMENTS.md, PROJECT.md, config.json, codebase/, research/
    (excluding wisenet-postman.json), phases/01/, phases/02/, and 03-CONTEXT.md are all in git.
    wisenet-postman.json does NOT appear in any commit.
    npm run build exits 0 after all three sub-task commits.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| prod-snapshot → active repo | Files copied verbatim from prod-snapshot; must not include embedded secrets |
| git staging gate | .env*, .clasprc.local.json must never cross from working tree into staged index |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-00-1 | Information Disclosure | Sub-task 2 git add web/ | mitigate | Verify `web/.gitignore` lists `.env`, `.env.*`, `.env.local` before staging; run `git diff --staged --name-only \| grep -i ".env"` and assert empty result before committing |
| T-03-00-2 | Information Disclosure | Sub-task 1 prod-snapshot copy | mitigate | Read each source file before copying; confirm no hardcoded secrets (API keys, DB URLs) in the 7 Sheets-era route files. Sheets-era routes use `getSheetsEnv()` which reads process.env — no inline credentials expected |
| T-03-00-3 | Information Disclosure | Sub-task 3 .planning/research/ staging | mitigate | Explicitly exclude `wisenet-postman.json` via `git restore --staged` after bulk add; verify with `git show HEAD --name-only \| grep wisenet-postman` returns empty |
</threat_model>

<verification>
After all three commits:
1. `git log --oneline -3` shows three commits with messages matching the specified format
2. `cd web && npm run build` exits 0 (restored files are Sheets-era TypeScript that already compiled in prod-snapshot)
3. `git status` shows no untracked files in web/ or .planning/ that should be tracked
4. `git log --all --full-history -- "web/.env" | wc -l` returns 0
5. `git show HEAD~2 --name-only | grep "api/dashboard/route.ts"` confirms Sub-task 1 landed 7 files
</verification>

<success_criteria>
- Three atomic commits on main, in order: chore(03-00) → feat(02-retroactive) → docs(planning)
- `cd web && npm run build` exits 0 after all three commits
- `cd web && npm test -- --run` exits 0 (164+ tests pass — Phase 2 baseline unchanged)
- No .env file, .clasprc.local.json, or wisenet-postman.json appears in any of the three commits
- `git status` shows a clean working tree (only gitignored files remain untracked)
</success_criteria>

<output>
After completion, create `.planning/phases/03-service-cutover/03-00-SUMMARY.md` containing:
- Which files were in each commit (Sub-task 1 / 2 / 3)
- Commit SHAs for all three commits
- Any deviations from plan (split commits in Sub-task 2, files not found, etc.)
- Build result after Sub-task 3 commit
</output>
