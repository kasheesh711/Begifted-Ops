---
phase: 03-service-cutover
plan: "08"
type: execute
wave: 5
depends_on: ["03-07"]
files_modified:
  - web/src/lib/sheets/client.ts
  - web/src/lib/sheets/source-loader.ts
  - web/src/lib/sheets/actions.ts
  - web/src/lib/sheets/inactive-students.ts
  - web/src/lib/cache/memory-cache.ts
  - web/src/lib/dashboard/snapshot-store.ts
  - web/src/lib/dashboard/build.ts
  - web/scripts/compare-live.ts
  - web/scripts/ensure-action-sheets.ts
  - web/scripts/ensure-inactive-sheet.ts
autonomous: true
requirements:
  - SVC-05

must_haves:
  truths:
    - "web/src/lib/sheets/ directory is deleted"
    - "web/src/lib/cache/memory-cache.ts is deleted"
    - "web/src/lib/dashboard/snapshot-store.ts is deleted"
    - "web/src/lib/dashboard/build.ts is deleted"
    - "web/scripts/compare-live.ts is deleted"
    - "web/scripts/ensure-action-sheets.ts is deleted"
    - "web/scripts/ensure-inactive-sheet.ts is deleted"
    - "npm run build succeeds after all deletions (zero references remaining)"
    - "npm test -- --run exits 0 after deletions"
  artifacts: []
  key_links:
    - from: "DELETED: web/src/lib/sheets/"
      to: "build passes"
      via: "zero imports of lib/sheets in any remaining file"
      pattern: "!grep -r lib/sheets web/src/"
---

<objective>
Delete all Sheets-era source files that are no longer referenced after the Wave 1-4 cutover. This is the "subtraction PR" per D-33: additive commits first, deletion last.

Purpose: Removes dead code that would otherwise confuse future contributors and drift from the Postgres truth. Each file must have zero remaining references before deletion — verified by `npm run build`.

Files to delete:
- `web/src/lib/sheets/` (4 files: client.ts, source-loader.ts, actions.ts, inactive-students.ts)
- `web/src/lib/cache/memory-cache.ts` (only consumed by lib/sheets/ which is also deleted)
- `web/src/lib/dashboard/snapshot-store.ts` (per-process state, broken on serverless; replaced by Postgres)
- `web/src/lib/dashboard/build.ts` (Sheets-only uncached path; absorbed into service.ts in Wave 2)
- `web/scripts/compare-live.ts` (Sheets↔Apps Script parity tool; purpose retired with Sheets)
- `web/scripts/ensure-action-sheets.ts` (googleapis-dependent; Sheets tabs no longer the truth)
- `web/scripts/ensure-inactive-sheet.ts` (googleapis-dependent)

DO NOT delete yet:
- `web/src/lib/dashboard/actions.ts` — this file was REWRITTEN in Wave 3 (not the Sheets-era version)
- `web/src/lib/dashboard/health-state.ts` — still used by service.ts
- Any files in lib/wisenet/ or lib/db/ — Phase 2 code, keep forever
</objective>

<execution_context>
@/Users/kevinhsieh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kevinhsieh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/kevinhsieh/Developer/Credit Control/Begifted-Ops/.planning/ROADMAP.md
@/Users/kevinhsieh/Developer/Credit Control/Begifted-Ops/.planning/phases/03-service-cutover/03-CONTEXT.md
@/Users/kevinhsieh/Developer/Credit Control/Begifted-Ops/.planning/phases/03-service-cutover/03-PATTERNS.md

From 03-PATTERNS.md §Deletions:
- lib/sheets/client.ts: DELETE — googleapis JWT auth, replaced by Wisenet
- lib/sheets/source-loader.ts: DELETE — Sheets read path
- lib/sheets/actions.ts: DELETE — Sheets-write action helpers
- lib/sheets/inactive-students.ts: DELETE — InactiveStudents tab read/write
- lib/cache/memory-cache.ts: DELETE — only consumers were lib/sheets/*
- lib/dashboard/snapshot-store.ts: DELETE — per-process state, broken on serverless
- lib/dashboard/build.ts: DELETE — Sheets-only uncached path (body absorbed into service.ts)
- scripts/compare-live.ts: DELETE — Sheets↔Apps Script parity tool
- scripts/ensure-action-sheets.ts: DELETE — googleapis-dependent
- scripts/ensure-inactive-sheet.ts: DELETE — googleapis-dependent

D-33 per-commit requirement: build must pass at each step.
D-33: commits 8+9 are pure subtraction; only land after commit 7 state is verified.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit references, then delete lib/sheets/ lib/cache/ lib/dashboard/{snapshot-store,build}</name>
  <files>
    web/src/lib/sheets/client.ts
    web/src/lib/sheets/source-loader.ts
    web/src/lib/sheets/actions.ts
    web/src/lib/sheets/inactive-students.ts
    web/src/lib/cache/memory-cache.ts
    web/src/lib/dashboard/snapshot-store.ts
    web/src/lib/dashboard/build.ts
  </files>
  <read_first>
    AUDIT STEP (do NOT delete until all references are confirmed zero):
    1. Run: grep -r "lib/sheets" web/src/ --include="*.ts" --include="*.tsx" -l
       (must return empty — if any file still imports lib/sheets, fix that import first)
    2. Run: grep -r "memory-cache" web/src/ --include="*.ts" --include="*.tsx" -l
       (must return empty)
    3. Run: grep -r "snapshot-store" web/src/ --include="*.ts" --include="*.tsx" -l
       (must return empty)
    4. Run: grep -r "lib/dashboard/build" web/src/ --include="*.ts" --include="*.tsx" -l
       (must return empty)
    5. Run: grep -r "invalidateDashboardPayloadCache" web/src/ --include="*.ts" --include="*.tsx" -l
       (must return empty — this export was on the old service.ts and must be fully removed by now)

    If any reference still exists, DO NOT DELETE. Fix the import in the referencing file first.
  </read_first>
  <action>
    Only proceed after all 5 audit greps above return empty.

    1. Delete the lib/sheets/ directory:
       git rm -r web/src/lib/sheets/

    2. Delete memory-cache.ts:
       git rm web/src/lib/cache/memory-cache.ts

    3. Delete snapshot-store.ts:
       git rm web/src/lib/dashboard/snapshot-store.ts

    4. Delete build.ts:
       git rm web/src/lib/dashboard/build.ts

    5. Run: cd web && npm run build
       (MUST exit 0 — if build fails, a reference was missed; identify and fix before committing)

    6. Run: cd web && npx tsc --noEmit
       (must exit 0)

    7. Run: cd web && npm test -- --run
       (must exit 0 — tests that imported lib/sheets may now fail; if so, check which tests reference
       them and fix or remove those test references)

    8. Commit:
       git commit -m "chore(03-08): delete lib/sheets + memory-cache + snapshot-store + build.ts"
  </action>
  <verify>
    <automated>
      # Directories and files must not exist:
      test ! -d web/src/lib/sheets && echo "sheets deleted"
      test ! -f web/src/lib/cache/memory-cache.ts && echo "memory-cache deleted"
      test ! -f web/src/lib/dashboard/snapshot-store.ts && echo "snapshot-store deleted"
      test ! -f web/src/lib/dashboard/build.ts && echo "build.ts deleted"
      # Zero remaining references:
      grep -r "lib/sheets" web/src/ --include="*.ts" --include="*.tsx" | wc -l
      # (must be 0)
      cd web && npm run build && echo "build: OK"
      cd web && npm test -- --run && echo "tests: OK"
    </automated>
  </verify>
  <done>
    lib/sheets/, memory-cache.ts, snapshot-store.ts, build.ts all deleted.
    Zero remaining references in web/src/. npm run build exits 0. Tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete Sheets-era scripts (compare-live, ensure-action-sheets, ensure-inactive-sheet)</name>
  <files>
    web/scripts/compare-live.ts
    web/scripts/ensure-action-sheets.ts
    web/scripts/ensure-inactive-sheet.ts
  </files>
  <read_first>
    1. web/package.json — read the scripts section to identify which npm script names reference these files
       (compare-live, ensure-action-sheets, ensure-inactive-sheet are npm scripts that reference these files)
    2. Check that nothing outside these scripts references the files:
       grep -r "compare-live\|ensure-action-sheets\|ensure-inactive-sheet" web/src/ 2>/dev/null | wc -l
       (must be 0)
  </read_first>
  <action>
    1. Delete the three scripts:
       git rm web/scripts/compare-live.ts
       git rm web/scripts/ensure-action-sheets.ts
       git rm web/scripts/ensure-inactive-sheet.ts

    2. Remove the corresponding npm scripts from web/package.json:
       Remove these entries from the "scripts" section:
       - "compare-live": "tsx scripts/compare-live.ts"
       - "ensure-action-sheets": "tsx scripts/ensure-action-sheets.ts"
       - "ensure-inactive-sheet": "tsx scripts/ensure-inactive-sheet.ts"

       NOTE: The package.json scripts section may have other entries; only remove these three.

    3. Run: cd web && npm run build (must still exit 0)
    4. Run: cd web && npm test -- --run (must still exit 0)

    5. Commit:
       git add web/package.json
       git commit -m "chore(03-08): delete compare-live + ensure-* scripts (Sheets-era tooling retired)"
  </action>
  <verify>
    <automated>
      test ! -f web/scripts/compare-live.ts && echo "compare-live deleted"
      test ! -f web/scripts/ensure-action-sheets.ts && echo "ensure-action-sheets deleted"
      test ! -f web/scripts/ensure-inactive-sheet.ts && echo "ensure-inactive-sheet deleted"
      grep -n "compare-live\|ensure-action-sheets\|ensure-inactive-sheet" web/package.json | wc -l
      # (must be 0)
      cd web && npm run build && echo "build: OK"
      cd web && npm test -- --run && echo "tests: OK"
    </automated>
  </verify>
  <done>
    Three scripts deleted. npm script entries removed from package.json.
    npm run build exits 0. Tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deletion → build | Deleting files that are still imported causes build failure; audit step prevents this |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08-1 | Denial of Service | deletion accidentally removes still-referenced code | mitigate | Pre-deletion audit: all 5 grep commands must return 0 matches before any git rm. `npm run build` must exit 0 after each deletion batch. Build failure = blocker; do not commit broken state |
</threat_model>

<verification>
1. `test ! -d web/src/lib/sheets` returns true
2. `test ! -f web/src/lib/cache/memory-cache.ts` returns true
3. `test ! -f web/src/lib/dashboard/snapshot-store.ts` returns true
4. `test ! -f web/src/lib/dashboard/build.ts` returns true
5. `grep -r "lib/sheets" web/src/ --include="*.ts" --include="*.tsx" | wc -l` returns 0
6. `grep -r "memory-cache" web/src/ --include="*.ts" --include="*.tsx" | wc -l` returns 0
7. `grep "compare-live\|ensure-action-sheets\|ensure-inactive-sheet" web/package.json | wc -l` returns 0
8. `cd web && npm run build` exits 0
9. `cd web && npm test -- --run` exits 0
</verification>

<success_criteria>
- lib/sheets/ (4 files), memory-cache.ts, snapshot-store.ts, build.ts deleted
- compare-live.ts, ensure-action-sheets.ts, ensure-inactive-sheet.ts deleted
- Corresponding npm scripts removed from package.json
- Zero remaining references to deleted files in web/src/
- npm run build exits 0 after all deletions
- Test suite passes
- Commit messages include "chore(03-08):" prefix per D-33
</success_criteria>

<output>
After completion, create `.planning/phases/03-service-cutover/03-08-SUMMARY.md` with:
- Whether any unexpected import references were found during audit
- Which npm scripts were removed from package.json
- Build result (any warnings from removal)
- Commit SHAs for both commits
</output>
