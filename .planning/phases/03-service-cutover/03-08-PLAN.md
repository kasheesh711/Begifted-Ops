---
phase: 03-service-cutover
plan: 08
type: execute
wave: 5
depends_on:
  - 03-03
  - 03-04
  - 03-05
  - 03-06
  - 03-07
files_modified:
  - web/src/lib/sheets/client.ts
  - web/src/lib/sheets/source-loader.ts
  - web/src/lib/sheets/actions.ts
  - web/src/lib/sheets/inactive-students.ts
  - web/src/lib/cache/memory-cache.ts
  - web/src/lib/dashboard/snapshot-store.ts
  - web/src/lib/dashboard/build.ts
  - web/scripts/compare-live.ts
  - web/scripts/ensure-action-sheets.ts
  - web/scripts/ensure-inactive-sheet.ts
requirements:
  - SVC-05
autonomous: true
must_haves:
  truths:
    - "All 4 web/src/lib/sheets/*.ts files are removed from the repo"
    - "web/src/lib/cache/memory-cache.ts is removed"
    - "web/src/lib/dashboard/snapshot-store.ts is removed"
    - "web/src/lib/dashboard/build.ts is removed (orchestration body absorbed into service.ts in Plan 03-03)"
    - "web/scripts/compare-live.ts is removed (Sheets parity tool, no longer meaningful)"
    - "web/scripts/ensure-action-sheets.ts and ensure-inactive-sheet.ts are removed (googleapis-dependent tab creators)"
    - "No remaining references to any deleted module survive in the codebase (grep clean)"
    - "cd web && npm run build still exits 0 after deletion"
  artifacts:
    - path: "web/src/lib/sheets/"
      provides: "(REMOVED — directory no longer exists)"
      contains: "(absent)"
    - path: "web/src/lib/cache/memory-cache.ts"
      provides: "(REMOVED)"
      contains: "(absent)"
  key_links:
    - from: "post-deletion repo"
      to: "Phase 3 service.ts + actions.ts"
      via: "All imports resolve through new Wisenet + Postgres surface only"
      pattern: "lib/wisenet|lib/db"
---

<objective>
Delete all Sheets-era code and supporting per-process caches now that Plans 03-03 through 03-07 have proven the new Wisenet + Postgres surface is reachable from every consumer (SVC-05). This is the first deletion-only commit per D-33 commit-8, landing AFTER the verification gates in Plan 03-07 prove the cutover is functional.

Purpose: SVC-05 retires the legacy code path. Per D-33 ordering, deletion lands AFTER the cutover is proven via:
- Plan 03-03: service.ts no longer imports from `lib/sheets/`
- Plan 03-05: 5 route handlers no longer import from `lib/sheets/`
- Plan 03-07: TEST-03 + TEST-06 prove Postgres + Wisenet pipeline correctness end-to-end

Until this plan runs, the `lib/sheets/` directory + `memory-cache.ts` + `snapshot-store.ts` + `build.ts` continue to exist on disk as orphan code (no imports, dead). This plan reaps that dead code.
Output: 10 deletions across 4 directories. After this plan, only the new Wisenet + Postgres data path remains. `npm run build` proves no orphan imports survived.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-service-cutover/03-CONTEXT.md
@.planning/phases/03-service-cutover/03-PATTERNS.md
@web/src/lib/sheets/
@web/src/lib/cache/
@web/src/lib/dashboard/snapshot-store.ts
@web/src/lib/dashboard/build.ts
@web/scripts/compare-live.ts

<interfaces>
<!-- Pre-deletion ownership map per Pattern Map §Deletions table -->

| File | Why | Downstream Impact (already addressed by prior plans) |
|------|-----|-----------------------------------------------------|
| web/src/lib/sheets/client.ts | googleapis JWT auth, replaced by Wisenet | service.ts (Plan 03-03) no longer imports from `lib/sheets/`; route handlers (Plan 03-05) no longer call `getSheetsClient` |
| web/src/lib/sheets/source-loader.ts | Sheets read path, replaced by buildDashboardSourcesFromWisenet | service.ts (Plan 03-03) calls Wisenet mapper directly |
| web/src/lib/sheets/actions.ts | Sheets-write helpers (setStudentActionInSheets etc.) | actions/route.ts (Plan 03-05 Task 1) imports from @/lib/dashboard/actions instead |
| web/src/lib/sheets/inactive-students.ts | InactiveStudents tab read/write | listInactive/markInactive/clearInactive in @/lib/db/queries replace |
| web/src/lib/cache/memory-cache.ts | Only consumers were lib/sheets/* | cascade with sheets/ deletion |
| web/src/lib/dashboard/snapshot-store.ts | Per-process state, broken on serverless | service.ts (Plan 03-03) passes null for prior snapshot — accepted tradeoff |
| web/src/lib/dashboard/build.ts | Sheets-only uncached path | service.ts (Plan 03-03) absorbed orchestration body inline with directive |
| web/scripts/compare-live.ts | Sheets↔Apps Script parity tool, post-cutover meaningless | package.json compare-live script entry removed in Plan 03-09 |
| web/scripts/ensure-action-sheets.ts | googleapis-dependent | package.json ensure-action-sheets script entry removed in Plan 03-09 |
| web/scripts/ensure-inactive-sheet.ts | googleapis-dependent | package.json ensure-inactive-sheet script entry removed in Plan 03-09 |

<!-- Cross-file rules guarding this deletion -->
Pattern Map §Cross-File Consistency Constraints rule #8: "lib/sheets/* and lib/cache/memory-cache.ts deleted ONLY in commit 8 (D-33) — earlier commits keep them coexisting with the new code paths."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-deletion grep audit (verify no live imports remain)</name>
  <read_first>
    - .planning/phases/03-service-cutover/03-PATTERNS.md §Deletions table + §Cross-File Consistency Constraints rule #8
  </read_first>
  <files></files>
  <action>
    BEFORE deleting any file, run a grep audit across `web/src/` (excluding `web/src/test/fixtures/` and `web/scripts/`) to confirm Plans 03-03 through 03-07 successfully removed every `lib/sheets/`, `lib/cache/memory-cache`, `snapshot-store`, and `build` import.

    Run these greps from `web/`:

    ```bash
    cd web
    # 1. lib/sheets references in src/ (excluding the doomed files themselves)
    grep -rn "from \"@/lib/sheets" src/ --exclude-dir=test/fixtures || echo "OK: no lib/sheets imports outside doomed files"

    # 2. memory-cache references
    grep -rn "from \"@/lib/cache/memory-cache\"" src/ || echo "OK: no memory-cache imports"
    grep -rn "from \"@/lib/cache/" src/ | grep -v "memory-cache" || echo "OK: lib/cache/ has no other consumers"

    # 3. snapshot-store references
    grep -rn "from \"@/lib/dashboard/snapshot-store\"" src/ || echo "OK: no snapshot-store imports"

    # 4. build.ts (the Sheets-era getDashboardPayloadUncached site)
    grep -rn "from \"@/lib/dashboard/build\"" src/ || echo "OK: no build.ts imports"
    grep -rn "buildDashboardPayloadUncached" src/ || echo "OK: no buildDashboardPayloadUncached references"
    ```

    EXPECTED RESULT for each grep: either zero matches OR matches ONLY inside the doomed files themselves (e.g., `lib/sheets/client.ts` may transiently import `lib/sheets/inactive-students.ts` — that's expected; both are about to be deleted).

    If ANY of these greps return matches in non-doomed files, STOP and revise the upstream plan that should have removed the import. Do NOT proceed to deletion. The `npm run build` post-deletion would fail with unresolved imports.

    Document the pre-deletion grep evidence in the SUMMARY for this plan.
  </action>
  <acceptance_criteria>
    - All 5 grep commands above return either "OK: ..." messages OR show matches confined to the doomed files themselves.
    - Specifically: `grep -l "from \"@/lib/sheets" web/src --include="*.ts" --include="*.tsx" -r | grep -v "src/lib/sheets/"` returns 0 lines (no NON-doomed file imports from `lib/sheets`).
    - `grep -l "from \"@/lib/cache/memory-cache\"" web/src --include="*.ts" --include="*.tsx" -r | grep -v "src/lib/cache/"` returns 0 lines.
    - `grep -l "from \"@/lib/dashboard/snapshot-store\"" web/src --include="*.ts" --include="*.tsx" -r | grep -v "src/lib/dashboard/snapshot-store"` returns 0 lines.
    - `grep -l "from \"@/lib/dashboard/build\"" web/src --include="*.ts" --include="*.tsx" -r | grep -v "src/lib/dashboard/build"` returns 0 lines.
  </acceptance_criteria>
  <verify>
    <automated>cd "/Users/kevinhsieh/Developer/Credit Control/Begifted-Ops/web" && (! grep -l "from \"@/lib/sheets" src --include="*.ts" --include="*.tsx" -r | grep -v "src/lib/sheets/") && (! grep -l "from \"@/lib/cache/memory-cache\"" src --include="*.ts" --include="*.tsx" -r | grep -v "src/lib/cache/") && (! grep -l "from \"@/lib/dashboard/snapshot-store\"" src --include="*.ts" --include="*.tsx" -r | grep -v "src/lib/dashboard/snapshot-store") && (! grep -l "from \"@/lib/dashboard/build\"" src --include="*.ts" --include="*.tsx" -r | grep -v "src/lib/dashboard/build")</automated>
  </verify>
  <done>
    Audit confirms all consumer imports were rewired by Plans 03-03/03-05; deletion is safe.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete legacy lib/sheets, lib/cache, dashboard/build + snapshot-store, scripts/compare-live + ensure-*</name>
  <read_first>
    - Task 1 acceptance criteria — must be green before this task runs
  </read_first>
  <files>
    web/src/lib/sheets/client.ts
    web/src/lib/sheets/source-loader.ts
    web/src/lib/sheets/actions.ts
    web/src/lib/sheets/inactive-students.ts
    web/src/lib/cache/memory-cache.ts
    web/src/lib/dashboard/snapshot-store.ts
    web/src/lib/dashboard/build.ts
    web/scripts/compare-live.ts
    web/scripts/ensure-action-sheets.ts
    web/scripts/ensure-inactive-sheet.ts
  </files>
  <action>
    Delete the listed files in a single commit. After deletion, also remove the directories `web/src/lib/sheets/` and `web/src/lib/cache/` if they are now empty (no remaining files).

    Use `git rm` so the deletion shows in `git status` as the staged removal:

    ```bash
    cd web
    git rm src/lib/sheets/client.ts \
           src/lib/sheets/source-loader.ts \
           src/lib/sheets/actions.ts \
           src/lib/sheets/inactive-students.ts \
           src/lib/cache/memory-cache.ts \
           src/lib/dashboard/snapshot-store.ts \
           src/lib/dashboard/build.ts \
           scripts/compare-live.ts \
           scripts/ensure-action-sheets.ts \
           scripts/ensure-inactive-sheet.ts

    # Remove now-empty directories (if any)
    rmdir src/lib/sheets 2>/dev/null || true
    rmdir src/lib/cache 2>/dev/null || true
    ```

    If `web/src/lib/sheets/` or `web/src/lib/cache/` contained any OTHER files not in the deletion list (e.g., a `types.ts` or `README.md`), STOP and audit them before deletion. Per Pattern Map §Deletions table, those are the only 4 files in `lib/sheets/` and 1 file in `lib/cache/`; if any unknown file exists, decide whether it's a stranded artifact (delete) or an additional consumer (keep + document why).

    After deletion, run:
    - `cd web && npx tsc --noEmit` — should exit 0 (no orphan imports if Task 1 audit was green).
    - `cd web && npm run build` — should exit 0 (Next.js build catches any runtime imports tsc may have missed, e.g. dynamic imports).
    - `cd web && npm test -- --run` — should exit 0 with 176 tests (preserved from Plan 03-07).

    If any of these commands fail, the deletion was premature; one or more upstream plans (03-03, 03-04, 03-05) didn't fully migrate a consumer. Stop, investigate, and revise the relevant earlier plan via gap-closure mode.
  </action>
  <acceptance_criteria>
    - `! test -f web/src/lib/sheets/client.ts` (file gone).
    - `! test -f web/src/lib/sheets/source-loader.ts`.
    - `! test -f web/src/lib/sheets/actions.ts`.
    - `! test -f web/src/lib/sheets/inactive-students.ts`.
    - `! test -d web/src/lib/sheets || [ -z "$(ls -A web/src/lib/sheets 2>/dev/null)" ]` — directory either gone or empty.
    - `! test -f web/src/lib/cache/memory-cache.ts`.
    - `! test -f web/src/lib/dashboard/snapshot-store.ts`.
    - `! test -f web/src/lib/dashboard/build.ts`.
    - `! test -f web/scripts/compare-live.ts`.
    - `! test -f web/scripts/ensure-action-sheets.ts`.
    - `! test -f web/scripts/ensure-inactive-sheet.ts`.
    - `cd web && npx tsc --noEmit` exits 0.
    - `cd web && npm run build` exits 0.
    - `cd web && npm test -- --run` exits 0; total 176 tests preserved (no regression from Plan 03-07).
    - `git status` confirms 10 staged deletions (or however many actually existed pre-Plan-03-08; verify file presence pre-task to catch any that were already removed).
  </acceptance_criteria>
  <verify>
    <automated>cd "/Users/kevinhsieh/Developer/Credit Control/Begifted-Ops/web" && ! test -f src/lib/sheets/client.ts && ! test -f src/lib/sheets/source-loader.ts && ! test -f src/lib/sheets/actions.ts && ! test -f src/lib/sheets/inactive-students.ts && ! test -f src/lib/cache/memory-cache.ts && ! test -f src/lib/dashboard/snapshot-store.ts && ! test -f src/lib/dashboard/build.ts && ! test -f scripts/compare-live.ts && ! test -f scripts/ensure-action-sheets.ts && ! test -f scripts/ensure-inactive-sheet.ts && npx tsc --noEmit && npm run build && npm test -- --run</automated>
  </verify>
  <done>
    10 legacy files deleted; build green; 176 tests still pass; `lib/sheets/` and `lib/cache/` directories either gone or empty.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo state pre-cutover-PR-merge → post-cutover-PR-merge | The deletion is irreversible at merge time; only Vercel rollback (D-27) restores the deleted modules from prior deployment artifact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-32 | Repudiation | Premature deletion (consumer still imports a doomed file) | mitigate | Task 1 grep audit MUST be green before Task 2 runs. Acceptance criteria for Task 1 include 4 explicit grep checks. If audit fails, plan stops with explicit revision instruction. |
| T-03-33 | Tampering | Stranded `web/src/lib/sheets/types.ts` or unknown file in lib/sheets/ | mitigate | Task 2 action notes: "If `web/src/lib/sheets/` or `web/src/lib/cache/` contained any OTHER files not in the deletion list, STOP and audit." Pattern Map §Deletions confirms the 4-file inventory; any 5th file is anomalous. |
| T-03-34 | Information Disclosure | `compare-live.ts` deletion removes a tool that was reading from production Sheets | accept | The script was Sheets-side only; post-cutover the new Wisenet+Postgres source has no Sheets to compare against. The tool's purpose is gone. RESEARCH §Specifics confirms this: "post-cutover it has no Sheets path to compare". |
| T-03-35 | Denial of Service | Operator runs `npm run compare-live` after deletion expecting it to work | accept | Plan 03-09 removes the npm script entry; running `npm run compare-live` will produce a clear "command not found" error. README updates land in Phase 5 RETI-06. Interim: the missing script is self-documenting. |
</threat_model>

<verification>
- `cd web && npx tsc --noEmit` exits 0
- `cd web && npm run build` exits 0 — proves no orphan imports survived deletion
- `cd web && npm test -- --run` exits 0; 176 tests preserved
- `cd web && npm run lint:no-revalidate-max` exits 0 (the lint regex no longer needs an allowlist; deletion of `service.ts:20`-style code happened in Plan 03-03)
- `git status` shows the 10 staged deletions
</verification>

<success_criteria>
- Maps to ROADMAP Phase 3 SC#3 first half ("`lib/sheets/`, `lib/cache/memory-cache.ts`, `lib/dashboard/snapshot-store.ts`, `lib/dashboard/actions.ts` are deleted").
- The literal SVC-05 "delete `lib/dashboard/actions.ts`" was reconciled in Plan 03-04: that file's Sheets-era body was REPLACED by the `'use server'` facade, NOT deleted. SC#3 semantically holds because no Sheets-era code lives in `actions.ts` post-Plan-03-04.
</success_criteria>

<rollback>
Per D-27, Vercel "Promote Deployment" of the prior production build is the first-line rollback — that deployment artifact still contains all the deleted modules. Per-plan `git revert` of the deletion commit also fully restores the files (since `git rm` preserves history).

Critical: this plan is one of the few "one-way door" commits in Phase 3 (per RESEARCH §Specifics line 351). Once SVC-05 deletes lib/sheets and SVC-06 (Plan 03-09) removes googleapis, rolling FORWARD requires re-doing the rewire. The Vercel-dashboard rollback path is the only zero-cost recovery — git revert + push works but produces a build state where googleapis is in package.json but `lib/sheets/` is gone (intermediate); resolve by reverting both Plan 03-08 + 03-09 commits together.
</rollback>

<output>
After completion, create `.planning/phases/03-service-cutover/03-08-SUMMARY.md` capturing:
- Pre-deletion grep audit evidence (output of the 4 grep commands from Task 1)
- 10 deleted files inventory + their git status confirmation
- Whether `lib/sheets/` and `lib/cache/` directories were removed (empty) or remain (had a stranded file — flag for follow-up)
- Final test count (expect 176 preserved)
- Build duration before/after (informational; expect a slight reduction since fewer modules to compile)
</output>
